generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id                                        String                                 @id @default(cuid())
  email                                     String                                 @unique
  name                                      String?
  role                                      String                                 @default("user")
  phone                                     String?
  passwordHash                              String?                                @map("password")
  emailVerified                             DateTime?
  image                                     String?
  createdAt                                 DateTime                               @default(now())
  updatedAt                                 DateTime                               @updatedAt
  banned                                    Boolean?
  banReason                                 String?
  banExpires                                Int?
  note                                      String?
  activityLogs                              ActivityLog[]
  cart                                      CartItem[]
  assignedChats                             ChatConversation[]                     @relation("AssignedChats")
  userChats                                 ChatConversation[]                     @relation("UserChats")
  chatMessages                              ChatMessage[]
  deliveryProofs                            DeliveryProof[]
  inventoryReservations                     InventoryReservation[]
  orders                                    Order[]
  payrollEntries                            PayrollEntry[]
  payrollProfile                            PayrollProfile?
  refunds                                   Refund[]
  reviews                                   Review[]
  productQuestions                          ProductQuestion[]                      @relation("ProductQuestionsAsked")
  productQuestionAnswers                    ProductQuestion[]                      @relation("ProductQuestionsAnswered")
  serviceBookings                           ServiceBooking[]
  assignedShipments                         Shipment[]                             @relation("AssignedShipments")
  shipmentAssignmentsBy                     ShipmentAssignment[]                   @relation("ShipmentAssignmentBy")
  shipmentAssignmentsTo                     ShipmentAssignment[]                   @relation("ShipmentAssignmentTo")
  shipmentCostLogsCreated                   ShipmentCostLog[]
  addresses                                 UserAddress[]
  assignedUserRoles                         UserRole[]                             @relation("AssignedUserRoles")
  userRoles                                 UserRole[]                             @relation("UserRoles")
  assignedWarehouseMemberships              WarehouseMembership[]                  @relation("AssignedWarehouseMemberships")
  warehouseMemberships                      WarehouseMembership[]                  @relation("WarehouseMemberships")
  purchaseOrdersCreated                     PurchaseOrder[]                        @relation("PurchaseOrdersCreated")
  purchaseOrdersApproved                    PurchaseOrder[]                        @relation("PurchaseOrdersApproved")
  purchaseOrdersManagerApproved             PurchaseOrder[]                        @relation("PurchaseOrdersManagerApproved")
  purchaseOrdersCommitteeApproved           PurchaseOrder[]                        @relation("PurchaseOrdersCommitteeApproved")
  purchaseOrdersFinalApproved               PurchaseOrder[]                        @relation("PurchaseOrdersFinalApproved")
  purchaseOrdersRejected                    PurchaseOrder[]                        @relation("PurchaseOrdersRejected")
  purchaseOrderApprovalEvents               PurchaseOrderApprovalEvent[]           @relation("PurchaseOrderApprovalEvents")
  purchaseOrderNotificationsReceived        PurchaseOrderNotification[]            @relation("PurchaseOrderNotificationsReceived")
  purchaseOrderTermsTemplatesCreated        PurchaseOrderTermsTemplate[]           @relation("PurchaseOrderTermsTemplatesCreated")
  purchaseOrderTermsTemplatesUpdated        PurchaseOrderTermsTemplate[]           @relation("PurchaseOrderTermsTemplatesUpdated")
  purchaseOrderLandedCostsCreated           PurchaseOrderLandedCost[]              @relation("PurchaseOrderLandedCostsCreated")
  purchaseRequisitionsCreated               PurchaseRequisition[]                  @relation("PurchaseRequisitionsCreated")
  purchaseRequisitionsApproved              PurchaseRequisition[]                  @relation("PurchaseRequisitionsApproved")
  purchaseRequisitionsConverted             PurchaseRequisition[]                  @relation("PurchaseRequisitionsConverted")
  purchaseRequisitionsBudgetCleared         PurchaseRequisition[]                  @relation("PurchaseRequisitionsBudgetCleared")
  purchaseRequisitionsEndorsed              PurchaseRequisition[]                  @relation("PurchaseRequisitionsEndorsed")
  purchaseRequisitionsAssignedProcurement   PurchaseRequisition[]                  @relation("PurchaseRequisitionsAssignedProcurement")
  purchaseRequisitionAttachmentsUploaded    PurchaseRequisitionAttachment[]        @relation("PurchaseRequisitionAttachmentsUploaded")
  purchaseRequisitionApprovalEvents         PurchaseRequisitionApprovalEvent[]     @relation("PurchaseRequisitionApprovalEvents")
  purchaseRequisitionVersionsCreated        PurchaseRequisitionVersion[]           @relation("PurchaseRequisitionVersionsCreated")
  purchaseRequisitionNotificationsReceived  PurchaseRequisitionNotification[]      @relation("PurchaseRequisitionNotificationsReceived")
  materialRequestsCreated                   MaterialRequest[]                      @relation("MaterialRequestsCreated")
  materialRequestsSupervisorEndorsed        MaterialRequest[]                      @relation("MaterialRequestsSupervisorEndorsed")
  materialRequestsProjectManagerEndorsed    MaterialRequest[]                      @relation("MaterialRequestsProjectManagerEndorsed")
  materialRequestsAdminApproved             MaterialRequest[]                      @relation("MaterialRequestsAdminApproved")
  materialRequestsRejected                  MaterialRequest[]                      @relation("MaterialRequestsRejected")
  materialRequestAttachmentsUploaded        MaterialRequestAttachment[]            @relation("MaterialRequestAttachmentsUploaded")
  materialRequestApprovalEvents             MaterialRequestApprovalEvent[]         @relation("MaterialRequestApprovalEvents")
  materialReleasesIssued                    MaterialReleaseNote[]                  @relation("MaterialReleasesIssued")
  assetRegistersCreated                     AssetRegister[]                        @relation("AssetRegistersCreated")
  inventoryVerificationsCreated             InventoryVerification[]                @relation("InventoryVerificationsCreated")
  inventoryVerificationsApproved            InventoryVerification[]                @relation("InventoryVerificationsApproved")
  inventoryVerificationEvents               InventoryVerificationApprovalEvent[]   @relation("InventoryVerificationApprovalEvents")
  inventoryVerificationCommitteeMemberships InventoryVerificationCommitteeMember[] @relation("InventoryVerificationCommitteeMemberships")
  stockAlertsCreated                        ReorderAlert[]                         @relation("ReorderAlertsCreated")
  goodsReceiptsReceived                     GoodsReceipt[]                         @relation("GoodsReceiptsReceived")
  goodsReceiptsRequesterConfirmed           GoodsReceipt[]                         @relation("GoodsReceiptsRequesterConfirmed")
  goodsReceiptAttachmentsUploaded           GoodsReceiptAttachment[]               @relation("GoodsReceiptAttachmentsUploaded")
  goodsReceiptVendorEvaluations             GoodsReceiptVendorEvaluation[]         @relation("GoodsReceiptVendorEvaluationsBy")
  supplierReturnsCreated                    SupplierReturn[]                       @relation("SupplierReturnsCreated")
  supplierReturnsApproved                   SupplierReturn[]                       @relation("SupplierReturnsApproved")
  supplierReturnsDispatched                 SupplierReturn[]                       @relation("SupplierReturnsDispatched")
  supplierReturnsClosed                     SupplierReturn[]                       @relation("SupplierReturnsClosed")
  supplierInvoicesCreated                   SupplierInvoice[]                      @relation("SupplierInvoicesCreated")
  supplierInvoicesMatched                   SupplierInvoice[]                      @relation("SupplierInvoicesMatched")
  supplierInvoicePaymentHoldsReleased       SupplierInvoice[]                      @relation("SupplierInvoicePaymentHoldsReleased")
  supplierPaymentsCreated                   SupplierPayment[]                      @relation("SupplierPaymentsCreated")
  supplierLedgerEntriesCreated              SupplierLedgerEntry[]                  @relation("SupplierLedgerEntriesCreated")
  paymentRequestsCreated                    PaymentRequest[]                       @relation("PaymentRequestsCreated")
  paymentRequestsManagerApproved            PaymentRequest[]                       @relation("PaymentRequestsManagerApproved")
  paymentRequestsFinanceApproved            PaymentRequest[]                       @relation("PaymentRequestsFinanceApproved")
  paymentRequestsTreasuryProcessed          PaymentRequest[]                       @relation("PaymentRequestsTreasuryProcessed")
  paymentRequestsRejected                   PaymentRequest[]                       @relation("PaymentRequestsRejected")
  paymentRequestsCancelled                  PaymentRequest[]                       @relation("PaymentRequestsCancelled")
  paymentRequestApprovalEvents              PaymentRequestApprovalEvent[]          @relation("PaymentRequestApprovalEvents")
  paymentRequestNotificationsReceived       PaymentRequestNotification[]           @relation("PaymentRequestNotificationsReceived")
  paymentRequestNotificationsCreated        PaymentRequestNotification[]           @relation("PaymentRequestNotificationsCreatedBy")
  warehouseTransfersCreated                 WarehouseTransfer[]                    @relation("WarehouseTransfersCreated")
  warehouseTransfersApproved                WarehouseTransfer[]                    @relation("WarehouseTransfersApproved")
  warehouseTransfersDispatched              WarehouseTransfer[]                    @relation("WarehouseTransfersDispatched")
  warehouseTransfersReceived                WarehouseTransfer[]                    @relation("WarehouseTransfersReceived")
  rfqsCreated                               Rfq[]                                  @relation("RfqsCreated")
  rfqsApproved                              Rfq[]                                  @relation("RfqsApproved")
  rfqAttachmentsUploaded                    RfqAttachment[]                        @relation("RfqAttachmentsUploaded")
  rfqCategoryTargetsCreated                 RfqCategoryTarget[]                    @relation("RfqCategoryTargetsCreated")
  rfqNotificationsCreated                   RfqNotification[]                      @relation("RfqNotificationsCreated")
  rfqSupplierInvitesCreated                 RfqSupplierInvite[]                    @relation("RfqSupplierInvitesCreated")
  supplierQuotationsSubmitted               SupplierQuotation[]                    @relation("SupplierQuotationsSubmitted")
  supplierQuotationAttachmentsUploaded      SupplierQuotationAttachment[]          @relation("SupplierQuotationAttachmentsUploaded")
  rfqAwardsGranted                          RfqAward[]                             @relation("RfqAwardsGranted")
  comparativeStatementsCreated              ComparativeStatement[]                 @relation("ComparativeStatementsCreated")
  comparativeStatementsUpdated              ComparativeStatement[]                 @relation("ComparativeStatementsUpdated")
  comparativeStatementsManagerApproved      ComparativeStatement[]                 @relation("ComparativeStatementsManagerApproved")
  comparativeStatementsCommitteeApproved    ComparativeStatement[]                 @relation("ComparativeStatementsCommitteeApproved")
  comparativeStatementsFinalApproved        ComparativeStatement[]                 @relation("ComparativeStatementsFinalApproved")
  comparativeStatementsRejected             ComparativeStatement[]                 @relation("ComparativeStatementsRejected")
  comparativeStatementApprovalEvents        ComparativeStatementApprovalEvent[]    @relation("ComparativeStatementApprovalEvents")
  comparativeStatementNotificationsReceived ComparativeStatementNotification[]     @relation("ComparativeStatementNotificationsReceived")
  supplierCategoriesCreated                 SupplierCategory[]                     @relation("SupplierCategoriesCreated")
  supplierCategorySuppliersCreated          SupplierCategorySupplier[]             @relation("SupplierCategorySuppliersCreated")
  replenishmentRulesCreated                 ReplenishmentRule[]                    @relation("ReplenishmentRulesCreated")
  replenishmentRulesUpdated                 ReplenishmentRule[]                    @relation("ReplenishmentRulesUpdated")
  supplierSlaPoliciesCreated                SupplierSlaPolicy[]                    @relation("SupplierSlaPoliciesCreated")
  supplierSlaPoliciesUpdated                SupplierSlaPolicy[]                    @relation("SupplierSlaPoliciesUpdated")
  supplierSlaBreachesEvaluated              SupplierSlaBreach[]                    @relation("SupplierSlaBreachesEvaluated")
  supplierSlaBreachesOwned                  SupplierSlaBreach[]                    @relation("SupplierSlaBreachesOwned")
  supplierSlaBreachesResolved               SupplierSlaBreach[]                    @relation("SupplierSlaBreachesResolved")
  supplierSlaBreachesDisputeRaised          SupplierSlaBreach[]                    @relation("SupplierSlaBreachesDisputeRaised")
  supplierSlaBreachesDisputeResolved        SupplierSlaBreach[]                    @relation("SupplierSlaBreachesDisputeResolved")
  supplierSlaFinancialRulesCreated          SupplierSlaFinancialRule[]             @relation("SupplierSlaFinancialRulesCreated")
  supplierSlaFinancialRulesUpdated          SupplierSlaFinancialRule[]             @relation("SupplierSlaFinancialRulesUpdated")
  supplierSlaTerminationCasesCreated        SupplierSlaTerminationCase[]           @relation("SupplierSlaTerminationCasesCreated")
  supplierSlaTerminationCasesOwned          SupplierSlaTerminationCase[]           @relation("SupplierSlaTerminationCasesOwned")
  supplierSlaTerminationCasesResolved       SupplierSlaTerminationCase[]           @relation("SupplierSlaTerminationCasesResolved")
  verifiedSupplierDocuments                 SupplierDocument[]                     @relation("SupplierDocumentsVerifiedBy")
  supplierProfileUpdateRequestsSubmitted    SupplierProfileUpdateRequest[]         @relation("SupplierProfileUpdateRequestsSubmitted")
  supplierProfileUpdateRequestsReviewed     SupplierProfileUpdateRequest[]         @relation("SupplierProfileUpdateRequestsReviewed")
  supplierFeedbackCreated                   SupplierFeedback[]                     @relation("SupplierFeedbackCreated")
  supplierPortalNotificationsCreated        SupplierPortalNotification[]           @relation("SupplierPortalNotificationsCreatedBy")
  supplierPortalNotifications               SupplierPortalNotification[]           @relation("SupplierPortalNotificationsUser")
  wishlist                                  Wishlist[]
  analyticsEvents                           AnalyticsEvent[]
  deliveryManProfile                        DeliveryManProfile?
  verifiedDeliveryMen                       DeliveryManProfile[]                   @relation("VerifiedDeliveryMen")
  assignedDeliveryMen                       DeliveryManProfile[]                   @relation("AssignedDeliveryMen")
  verifiedDeliveryManDocuments              DeliveryManDocument[]                  @relation("VerifiedDeliveryManDocuments")
  createdDeliveryAssignments                DeliveryAssignment[]                   @relation("CreatedDeliveryAssignments")
  deliveryAssignmentLogsCreated             DeliveryAssignmentLog[]                @relation("DeliveryAssignmentLogsCreated")
  pickupProofsCreated                       WarehousePickupProof[]                 @relation("WarehousePickupProofsCreated")
  supplierPortalAccess                      SupplierPortalAccess?                  @relation("SupplierPortalAccessUser")
  createdSupplierPortalAccesses             SupplierPortalAccess[]                 @relation("SupplierPortalAccessCreatedBy")
  investorPortalAccess                      InvestorPortalAccess?                  @relation("InvestorPortalAccessUser")
  createdInvestorPortalAccesses             InvestorPortalAccess[]                 @relation("InvestorPortalAccessCreatedBy")
  investorsCreated                          Investor[]                             @relation("InvestorsCreated")
  investorCapitalTransactionsCreated        InvestorCapitalTransaction[]           @relation("InvestorCapitalTransactionsCreated")
  investorProductAllocationsCreated         InvestorProductAllocation[]            @relation("InvestorProductAllocationsCreated")
  investorProfitRunsCreated                 InvestorProfitRun[]                    @relation("InvestorProfitRunsCreated")
  investorProfitRunsApproved                InvestorProfitRun[]                    @relation("InvestorProfitRunsApproved")
  investorProfitRunsPosted                  InvestorProfitRun[]                    @relation("InvestorProfitRunsPosted")
  investorProfitPayoutsCreated              InvestorProfitPayout[]                 @relation("InvestorProfitPayoutsCreated")
  investorProfitPayoutsApproved             InvestorProfitPayout[]                 @relation("InvestorProfitPayoutsApproved")
  investorProfitPayoutsRejected             InvestorProfitPayout[]                 @relation("InvestorProfitPayoutsRejected")
  investorProfitPayoutsPaid                 InvestorProfitPayout[]                 @relation("InvestorProfitPayoutsPaid")
  investorProfitPayoutsReleased             InvestorProfitPayout[]                 @relation("InvestorProfitPayoutsReleased")
  investorProfitPayoutsVoided               InvestorProfitPayout[]                 @relation("InvestorProfitPayoutsVoided")
  investorDocumentsUploaded                 InvestorDocument[]                     @relation("InvestorDocumentsUploadedBy")
  investorDocumentsReviewed                 InvestorDocument[]                     @relation("InvestorDocumentsReviewedBy")
  investorMasterChangeRequestsRequested     InvestorMasterChangeRequest[]          @relation("InvestorMasterChangeRequestsRequestedBy")
  investorMasterChangeRequestsReviewed      InvestorMasterChangeRequest[]          @relation("InvestorMasterChangeRequestsReviewedBy")
  investorsBeneficiaryVerified              Investor[]                             @relation("InvestorBeneficiaryVerifiedBy")
  investorProfileUpdateRequestsSubmitted    InvestorProfileUpdateRequest[]         @relation("InvestorProfileUpdateRequestsSubmittedBy")
  investorProfileUpdateRequestsReviewed     InvestorProfileUpdateRequest[]         @relation("InvestorProfileUpdateRequestsReviewedBy")
  investorWithdrawalRequestsSubmitted       InvestorWithdrawalRequest[]            @relation("InvestorWithdrawalRequestsSubmittedBy")
  investorWithdrawalRequestsReviewed        InvestorWithdrawalRequest[]            @relation("InvestorWithdrawalRequestsReviewedBy")
  investorWithdrawalRequestsSettled         InvestorWithdrawalRequest[]            @relation("InvestorWithdrawalRequestsSettledBy")
  investorPortalNotificationsCreated        InvestorPortalNotification[]           @relation("InvestorPortalNotificationsCreatedBy")
  investorInternalNotifications             InvestorInternalNotification[]         @relation("InvestorInternalNotificationsUser")
  investorInternalNotificationsCreated      InvestorInternalNotification[]         @relation("InvestorInternalNotificationsCreatedBy")
  investorStatementSchedulesCreated         InvestorStatementSchedule[]            @relation("InvestorStatementSchedulesCreatedBy")
  investorStatementSchedulesUpdated         InvestorStatementSchedule[]            @relation("InvestorStatementSchedulesUpdatedBy")
}

model UserAddress {
  id        Int      @id @default(autoincrement())
  userId    String
  label     String
  country   String
  district  String
  area      String
  details   String
  isDefault Boolean  @default(false)
  createdAt DateTime @default(now())
  user      User     @relation(fields: [userId], references: [id])
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

model Permission {
  id              String           @id @default(cuid())
  key             String           @unique
  description     String?
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt
  rolePermissions RolePermission[]
}

model Role {
  id              String           @id @default(cuid())
  name            String           @unique
  label           String
  description     String?
  isSystem        Boolean          @default(false)
  isImmutable     Boolean          @default(false)
  deletedAt       DateTime?
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt
  rolePermissions RolePermission[]
  userRoles       UserRole[]

  @@index([deletedAt])
}

model RolePermission {
  roleId       String
  permissionId String
  createdAt    DateTime   @default(now())
  permission   Permission @relation(fields: [permissionId], references: [id], onDelete: Cascade)
  role         Role       @relation(fields: [roleId], references: [id], onDelete: Cascade)

  @@id([roleId, permissionId])
  @@index([permissionId])
}

model UserRole {
  userId       String
  roleId       String
  assignedById String?
  createdAt    DateTime        @default(now())
  id           String          @id @default(cuid())
  scopeType    AccessScopeType @default(GLOBAL)
  updatedAt    DateTime        @updatedAt
  warehouseId  Int?
  assignedBy   User?           @relation("AssignedUserRoles", fields: [assignedById], references: [id])
  role         Role            @relation(fields: [roleId], references: [id], onDelete: Cascade)
  user         User            @relation("UserRoles", fields: [userId], references: [id], onDelete: Cascade)
  warehouse    Warehouse?      @relation(fields: [warehouseId], references: [id], onDelete: Cascade)

  @@unique([userId, roleId, scopeType, warehouseId])
  @@index([roleId])
  @@index([warehouseId])
  @@index([assignedById])
}

model Writer {
  id        Int       @id @default(autoincrement())
  name      String    @unique
  image     String?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  deleted   Boolean   @default(false)
  products  Product[]
}

model Publisher {
  id        Int       @id @default(autoincrement())
  name      String    @unique
  image     String?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  deleted   Boolean   @default(false)
  products  Product[]
}

model Category {
  id        Int        @id @default(autoincrement())
  name      String
  slug      String     @unique
  image     String?
  parentId  Int?
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt
  deleted   Boolean    @default(false)
  parent    Category?  @relation("CategoryToCategory", fields: [parentId], references: [id])
  children  Category[] @relation("CategoryToCategory")
  products  Product[]

  @@index([parentId])
}

model Brand {
  id        Int       @id @default(autoincrement())
  name      String    @unique
  slug      String    @unique
  logo      String?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  deleted   Boolean   @default(false)
  products  Product[]
}

model Product {
  id                      Int                               @id @default(autoincrement())
  name                    String
  slug                    String                            @unique
  type                    ProductType                       @default(PHYSICAL)
  sku                     String?                           @unique
  categoryId              Int
  brandId                 Int?
  writerId                Int?
  publisherId             Int?
  description             String
  shortDesc               String?
  basePrice               Decimal                           @db.Decimal(10, 2)
  originalPrice           Decimal?                          @db.Decimal(10, 2)
  flashSaleEnabled        Boolean                           @default(false)
  flashSalePrice          Decimal?                          @db.Decimal(10, 2)
  flashSaleStartsAt       DateTime?
  flashSaleEndsAt         DateTime?
  flashSaleSortOrder      Int                               @default(0)
  currency                String                            @default("USD") @db.VarChar(3)
  weight                  Float?
  dimensions              Json?
  VatClassId              Int?
  digitalAssetId          Int?
  serviceDurationMinutes  Int?
  serviceLocation         String?
  serviceOnlineLink       String?
  available               Boolean                           @default(true)
  featured                Boolean                           @default(false)
  image                   String?
  gallery                 String[]
  videoUrl                String?
  soldCount               Int                               @default(0)
  ratingAvg               Float                             @default(0)
  ratingCount             Int                               @default(0)
  deleted                 Boolean                           @default(false)
  createdAt               DateTime                          @default(now())
  updatedAt               DateTime                          @updatedAt
  lowStockThreshold       Int                               @default(10)
  inventoryItemClass      InventoryItemClass                @default(CONSUMABLE)
  requiresAssetTag        Boolean                           @default(false)
  bundleStockLimit        Int?
  cartItems               CartItem[]
  dailySnapshots          InventoryDailySnapshot[]
  inventoryLogs           InventoryLog[]
  warehouseDailySnapshots InventoryWarehouseDailySnapshot[]
  orderItems              OrderItem[]
  VatClass                VatClass?                         @relation(fields: [VatClassId], references: [id])
  brand                   Brand?                            @relation(fields: [brandId], references: [id])
  category                Category                          @relation(fields: [categoryId], references: [id])
  digitalAsset            DigitalAsset?                     @relation("ProductDigitalAsset", fields: [digitalAssetId], references: [id])
  publisher               Publisher?                        @relation(fields: [publisherId], references: [id])
  writer                  Writer?                           @relation(fields: [writerId], references: [id])
  attributes              ProductAttribute[]
  codes                   ProductCode[]
  variants                ProductVariant[]
  variantOptions          ProductVariantOption[]
  reviews                 Review[]
  questions               ProductQuestion[]
  serviceSlots            ServiceSlot[]
  wishlist                Wishlist[]
  bundleItems             ProductBundleItem[]               @relation("BundleProducts")
  includedInBundles       ProductBundleItem[]               @relation("IncludedProducts")

  @@index([deleted, available, createdAt])
  @@index([deleted, available, soldCount])
  @@index([deleted, available, basePrice])
  @@index([deleted, available, featured])
  @@index([deleted, available, flashSaleEnabled, flashSaleStartsAt, flashSaleEndsAt])
  @@index([categoryId, deleted, available])
  @@index([brandId, deleted, available])
  @@index([writerId, deleted, available])
  @@index([publisherId, deleted, available])
}

model ProductBundleItem {
  id        Int      @id @default(autoincrement())
  bundleId  Int
  productId Int
  quantity  Int      @default(1)
  sortOrder Int      @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  bundle  Product @relation("BundleProducts", fields: [bundleId], references: [id], onDelete: Cascade)
  product Product @relation("IncludedProducts", fields: [productId], references: [id], onDelete: Cascade)

  @@unique([bundleId, productId])
  @@index([bundleId])
  @@index([productId])
}

model ProductVariant {
  id                           Int                               @id @default(autoincrement())
  productId                    Int
  sku                          String
  price                        Decimal                           @db.Decimal(10, 2)
  currency                     String                            @default("USD") @db.VarChar(3)
  stock                        Int                               @default(0)
  digitalAssetId               Int?
  options                      Json
  colorImage                   String?
  createdAt                    DateTime                          @default(now())
  updatedAt                    DateTime                          @updatedAt
  isDefault                    Boolean                           @default(false)
  active                       Boolean                           @default(true)
  lowStockThreshold            Int                               @default(10)
  costPrice                    Decimal?                          @db.Decimal(10, 2)
  cartItems                    CartItem[]
  dailySnapshots               InventoryDailySnapshot[]
  inventoryLogs                InventoryLog[]
  warehouseDailySnapshots      InventoryWarehouseDailySnapshot[]
  orderItems                   OrderItem[]
  codes                        ProductCode[]
  digitalAsset                 DigitalAsset?                     @relation("VariantDigitalAsset", fields: [digitalAssetId], references: [id])
  product                      Product                           @relation(fields: [productId], references: [id])
  purchaseOrderItems           PurchaseOrderItem[]
  goodsReceiptItems            GoodsReceiptItem[]
  purchaseRequisitionItems     PurchaseRequisitionItem[]
  supplierInvoiceItems         SupplierInvoiceItem[]
  supplierReturnItems          SupplierReturnItem[]
  rfqItems                     RfqItem[]
  supplierQuotationItems       SupplierQuotationItem[]
  replenishmentRules           ReplenishmentRule[]
  warehouseTransferItems       WarehouseTransferItem[]
  stockLevels                  StockLevel[]
  materialRequestItems         MaterialRequestItem[]
  materialReleaseNoteItems     MaterialReleaseNoteItem[]
  assetRegisters               AssetRegister[]
  stockBinLevels               StockBinLevel[]
  inventoryVerificationLines   InventoryVerificationLine[]
  reorderAlerts                ReorderAlert[]
  investorAllocations          InvestorProductAllocation[]
  investorCapitalTransactions  InvestorCapitalTransaction[]
  investorProfitRunVariants    InvestorProfitRunVariant[]
  investorProfitRunAllocations InvestorProfitRunAllocation[]

  @@index([productId, active, stock])
}

model ProductVariantOption {
  id        Int                         @id @default(autoincrement())
  productId Int
  name      String
  position  Int                         @default(0)
  createdAt DateTime                    @default(now())
  updatedAt DateTime                    @updatedAt
  product   Product                     @relation(fields: [productId], references: [id])
  values    ProductVariantOptionValue[]

  @@unique([productId, name])
  @@index([productId, position])
}

model ProductVariantOptionValue {
  id        Int                  @id @default(autoincrement())
  optionId  Int
  value     String
  position  Int                  @default(0)
  createdAt DateTime             @default(now())
  updatedAt DateTime             @updatedAt
  option    ProductVariantOption @relation(fields: [optionId], references: [id], onDelete: Cascade)

  @@unique([optionId, value])
  @@index([optionId, position])
}

model ProductCode {
  id        Int                  @id @default(autoincrement())
  productId Int?
  variantId Int?
  kind      ProductCodeKind
  symbology ProductCodeSymbology
  value     String
  token     String?              @unique
  imageUrl  String?
  isPrimary Boolean              @default(true)
  status    String               @default("ACTIVE")
  metadata  Json?
  createdAt DateTime             @default(now())
  updatedAt DateTime             @updatedAt
  product   Product?             @relation(fields: [productId], references: [id], onDelete: Cascade)
  variant   ProductVariant?      @relation(fields: [variantId], references: [id], onDelete: Cascade)

  @@unique([symbology, value])
  @@index([productId])
  @@index([variantId])
  @@index([kind, status])
}

model DigitalAsset {
  id              Int               @id @default(autoincrement())
  title           String
  fileUrl         String
  storageProvider String?
  fileSize        Int?
  checksum        String?
  mimeType        String?
  maxDownloads    Int?
  expiresAt       DateTime?
  createdAt       DateTime          @default(now())
  updatedAt       DateTime          @updatedAt
  deliveries      DigitalDelivery[]
  products        Product[]         @relation("ProductDigitalAsset")
  variants        ProductVariant[]  @relation("VariantDigitalAsset")
}

model DigitalDelivery {
  id             Int           @id @default(autoincrement())
  orderItemId    Int
  digitalAssetId Int
  downloadLimit  Int?
  downloadCount  Int           @default(0)
  expiresAt      DateTime?
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  asset          DigitalAsset  @relation(fields: [digitalAssetId], references: [id])
  orderItem      OrderItem     @relation(fields: [orderItemId], references: [id])
  downloads      DownloadLog[]

  @@unique([orderItemId, digitalAssetId])
}

model DownloadLog {
  id                Int             @id @default(autoincrement())
  digitalDeliveryId Int
  ipAddress         String?
  userAgent         String?
  downloadedAt      DateTime        @default(now())
  delivery          DigitalDelivery @relation(fields: [digitalDeliveryId], references: [id])
}

model Attribute {
  id                Int                @id @default(autoincrement())
  name              String
  values            AttributeValue[]
  productAttributes ProductAttribute[]
}

model AttributeValue {
  id          Int       @id @default(autoincrement())
  value       String
  attributeId Int
  attribute   Attribute @relation(fields: [attributeId], references: [id])
}

model ProductAttribute {
  id          Int       @id @default(autoincrement())
  productId   Int
  attributeId Int
  value       String
  attribute   Attribute @relation(fields: [attributeId], references: [id])
  product     Product   @relation(fields: [productId], references: [id])

  @@index([attributeId, value], map: "Catalog_ProductAttribute_attribute_value_idx")
  @@index([productId, attributeId], map: "Catalog_ProductAttribute_product_attribute_idx")
}

model VatClass {
  id          Int       @id @default(autoincrement())
  name        String
  code        String    @unique
  description String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  products    Product[]
  rates       VatRate[]
}

model VatRate {
  id          Int       @id @default(autoincrement())
  VatClassId  Int
  countryCode String    @db.VarChar(2)
  regionCode  String?   @db.VarChar(10)
  rate        Decimal   @db.Decimal(6, 4)
  inclusive   Boolean   @default(false)
  startDate   DateTime?
  endDate     DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  VatClass    VatClass  @relation(fields: [VatClassId], references: [id])

  @@index([countryCode, regionCode])
}

model Order {
  id                    Int                    @id @default(autoincrement())
  userId                String?
  name                  String
  email                 String?
  phone_number          String
  alt_phone_number      String?
  country               String
  district              String
  area                  String
  address_details       String
  image                 String?
  payment_method        String
  order_date            DateTime               @default(now())
  total                 Decimal                @db.Decimal(10, 2)
  shipping_cost         Decimal                @db.Decimal(10, 2)
  grand_total           Decimal                @db.Decimal(10, 2)
  currency              String                 @default("BDT") @db.VarChar(3)
  Vat_total             Decimal?               @db.Decimal(10, 2)
  discount_total        Decimal?               @db.Decimal(10, 2)
  status                OrderStatus            @default(PENDING)
  paymentStatus         PaymentStatus          @default(UNPAID)
  transactionId         String?
  createdAt             DateTime               @default(now())
  updatedAt             DateTime               @updatedAt
  couponId              String?
  taxSnapshot           Json?
  deliveryProofs        DeliveryProof[]
  inventoryReservations InventoryReservation[]
  coupon                Coupon?                @relation(fields: [couponId], references: [id])
  user                  User?                  @relation(fields: [userId], references: [id])
  orderItems            OrderItem[]
  payments              Payment[]
  refunds               Refund[]
  shipments             Shipment?
  deliveryAssignments   DeliveryAssignment[]
}

model Payment {
  id                 Int          @id @default(autoincrement())
  orderId            Int?
  amount             Decimal?     @db.Decimal(10, 2)
  currency           String       @default("BDT") @db.VarChar(3)
  provider           String?
  status             PaymentState @default(INITIATED)
  externalId         String?      @unique
  paymentGatewayData Json?
  createdAt          DateTime     @default(now())
  updatedAt          DateTime     @updatedAt
  order              Order?       @relation(fields: [orderId], references: [id])
  refunds            Refund[]
}

model OrderItem {
  id                Int               @id @default(autoincrement())
  orderId           Int
  productId         Int
  variantId         Int?
  quantity          Int               @default(1)
  price             Decimal           @db.Decimal(10, 2)
  currency          String            @default("BDT") @db.VarChar(3)
  VatAmount         Decimal?          @db.Decimal(10, 2)
  discountAmount    Decimal?          @db.Decimal(10, 2)
  createdAt         DateTime          @default(now())
  updatedAt         DateTime          @updatedAt
  costPriceSnapshot Decimal?          @db.Decimal(10, 2)
  digitalDeliveries DigitalDelivery[]
  order             Order             @relation(fields: [orderId], references: [id])
  product           Product           @relation(fields: [productId], references: [id])
  variant           ProductVariant?   @relation(fields: [variantId], references: [id])
  refunds           Refund[]
  serviceBooking    ServiceBooking?   @relation("OrderItemServiceBooking")
  shipmentItems     ShipmentItem[]
}

model ServiceSlot {
  id          Int              @id @default(autoincrement())
  productId   Int
  startsAt    DateTime
  endsAt      DateTime
  capacity    Int              @default(1)
  bookedCount Int              @default(0)
  timezone    String?
  location    String?
  notes       String?
  createdAt   DateTime         @default(now())
  updatedAt   DateTime         @updatedAt
  bookings    ServiceBooking[]
  product     Product          @relation(fields: [productId], references: [id])
}

model ServiceBooking {
  id          Int           @id @default(autoincrement())
  slotId      Int
  orderItemId Int?          @unique
  userId      String?
  status      BookingStatus @default(PENDING)
  notes       String?
  createdAt   DateTime      @default(now())
  updatedAt   DateTime      @updatedAt
  orderItem   OrderItem?    @relation("OrderItemServiceBooking", fields: [orderItemId], references: [id])
  slot        ServiceSlot   @relation(fields: [slotId], references: [id])
  user        User?         @relation(fields: [userId], references: [id])

  @@index([userId])
}

model Review {
  id        Int      @id @default(autoincrement())
  rating    Int
  comment   String?
  productId Int
  feature   Boolean? @default(false)
  userId    String
  createdAt DateTime @default(now())
  product   Product  @relation(fields: [productId], references: [id])
  user      User     @relation(fields: [userId], references: [id])
}

model ProductQuestion {
  id           Int       @id @default(autoincrement())
  question     String
  answer       String?
  productId    Int
  userId       String
  answeredById String?
  createdAt    DateTime  @default(now())
  answeredAt   DateTime?
  product      Product   @relation(fields: [productId], references: [id], onDelete: Cascade)
  user         User      @relation("ProductQuestionsAsked", fields: [userId], references: [id], onDelete: Cascade)
  answeredBy   User?     @relation("ProductQuestionsAnswered", fields: [answeredById], references: [id], onDelete: SetNull)

  @@index([productId, createdAt])
  @@index([userId])
  @@index([answeredById])
}

model CartItem {
  id        Int             @id @default(autoincrement())
  userId    String
  productId Int
  quantity  Int             @default(1)
  variantId Int?
  product   Product         @relation(fields: [productId], references: [id])
  user      User            @relation(fields: [userId], references: [id])
  variant   ProductVariant? @relation(fields: [variantId], references: [id])

  @@unique([userId, productId, variantId])
  @@index([variantId])
}

model Wishlist {
  id        Int     @id @default(autoincrement())
  userId    String
  productId Int
  product   Product @relation(fields: [productId], references: [id])
  user      User    @relation(fields: [userId], references: [id])

  @@unique([userId, productId])
}

model Blog {
  id        Int      @id @default(autoincrement())
  slug      String   @unique
  title     String
  summary   String
  content   String
  date      DateTime
  author    String
  image     String
  ads       String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Shipment {
  id                              Int                  @id @default(autoincrement())
  orderId                         Int                  @unique
  warehouseId                     Int?
  courier                         String
  courierId                       Int?
  trackingNumber                  String?
  externalId                      String?
  trackingUrl                     String?
  status                          ShipmentStatus       @default(PENDING)
  courierStatus                   String?
  lastSyncedAt                    DateTime?
  shippedAt                       DateTime?
  expectedDate                    DateTime?
  deliveredAt                     DateTime?
  createdAt                       DateTime             @default(now())
  updatedAt                       DateTime             @updatedAt
  deliveryConfirmationPin         String?
  deliveryConfirmationRequestedAt DateTime?
  deliveryConfirmationToken       String?              @unique
  actualCost                      Decimal?             @db.Decimal(10, 2)
  assignedAt                      DateTime?
  assignedToUserId                String?
  dispatchNote                    String?
  estimatedCost                   Decimal?             @db.Decimal(10, 2)
  fuelCost                        Decimal?             @db.Decimal(10, 2)
  handlingCost                    Decimal?             @db.Decimal(10, 2)
  outForDeliveryAt                DateTime?
  packagingCost                   Decimal?             @db.Decimal(10, 2)
  pickedAt                        DateTime?
  priority                        Int                  @default(0)
  shippingRateId                  Int?
  thirdPartyCost                  Decimal?             @db.Decimal(10, 2)
  deliveryProof                   DeliveryProof?
  assignedTo                      User?                @relation("AssignedShipments", fields: [assignedToUserId], references: [id])
  courierRef                      Courier?             @relation("ShipmentToCourier", fields: [courierId], references: [id])
  order                           Order                @relation(fields: [orderId], references: [id])
  shippingRate                    ShippingRate?        @relation(fields: [shippingRateId], references: [id])
  warehouse                       Warehouse?           @relation(fields: [warehouseId], references: [id])
  assignments                     ShipmentAssignment[]
  costLogs                        ShipmentCostLog[]
  deliveryAssignments             DeliveryAssignment[]
  items                           ShipmentItem[]
  statusLogs                      ShipmentStatusLog[]

  @@index([courierId])
  @@index([shippingRateId])
  @@index([assignedToUserId])
  @@index([deliveryConfirmationRequestedAt])
}

model DeliveryProof {
  id                Int      @id @default(autoincrement())
  orderId           Int
  shipmentId        Int      @unique
  userId            String?
  tickReceived      Boolean
  tickCorrectItems  Boolean
  tickGoodCondition Boolean
  photoUrl          String?
  note              String?
  confirmedAt       DateTime @default(now())
  ipAddress         String?
  userAgent         String?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  order             Order    @relation(fields: [orderId], references: [id])
  shipment          Shipment @relation(fields: [shipmentId], references: [id])
  user              User?    @relation(fields: [userId], references: [id])

  @@index([orderId])
  @@index([userId])
  @@index([confirmedAt])
}

model Courier {
  id            Int            @id @default(autoincrement())
  name          String         @unique
  type          CourierType
  baseUrl       String
  apiKey        String?
  secretKey     String?
  clientId      String?
  isActive      Boolean        @default(true)
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt
  shipments     Shipment[]     @relation("ShipmentToCourier")
  shippingRates ShippingRate[]
}

model Warehouse {
  id                               Int                               @id @default(autoincrement())
  name                             String
  code                             String                            @unique
  address                          Json?
  isDefault                        Boolean                           @default(false)
  createdAt                        DateTime                          @default(now())
  updatedAt                        DateTime                          @updatedAt
  area                             String?
  country                          String?                           @default("BD")
  coverageRadiusKm                 Float?
  district                         String?
  division                         String?
  geoFence                         Json?
  isMapEnabled                     Boolean                           @default(true)
  latitude                         Float?
  locationNote                     String?
  longitude                        Float?
  mapLabel                         String?
  postCode                         String?
  inventoryDailySnapshots          InventoryDailySnapshot[]
  inventoryLogs                    InventoryLog[]
  inventoryWarehouseDailySnapshots InventoryWarehouseDailySnapshot[]
  payrollEntries                   PayrollEntry[]
  payrollProfiles                  PayrollProfile[]
  shipments                        Shipment[]
  shipmentAssignments              ShipmentAssignment[]
  shippingRates                    ShippingRate[]
  stockLevels                      StockLevel[]
  userRoles                        UserRole[]
  memberships                      WarehouseMembership[]
  purchaseOrders                   PurchaseOrder[]
  purchaseRequisitions             PurchaseRequisition[]
  materialRequests                 MaterialRequest[]
  materialReleases                 MaterialReleaseNote[]
  assetRegisters                   AssetRegister[]
  zones                            WarehouseZone[]
  aisles                           WarehouseAisle[]
  bins                             WarehouseBin[]
  stockBinLevels                   StockBinLevel[]
  inventoryVerifications           InventoryVerification[]
  reorderAlerts                    ReorderAlert[]
  rfqs                             Rfq[]
  comparativeStatements            ComparativeStatement[]
  goodsReceipts                    GoodsReceipt[]
  paymentRequests                  PaymentRequest[]
  supplierReturns                  SupplierReturn[]
  replenishmentRules               ReplenishmentRule[]
  transfersFrom                    WarehouseTransfer[]               @relation("WarehouseTransferSource")
  transfersTo                      WarehouseTransfer[]               @relation("WarehouseTransferDestination")
  deliveryMen                      DeliveryManProfile[]
  deliveryAssignments              DeliveryAssignment[]

  @@index([country, division, district, area])
  @@index([latitude, longitude])
}

model WarehouseMembership {
  id           String    @id @default(cuid())
  userId       String
  warehouseId  Int
  isPrimary    Boolean   @default(false)
  status       String    @default("ACTIVE")
  assignedById String?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  assignedBy   User?     @relation("AssignedWarehouseMemberships", fields: [assignedById], references: [id])
  user         User      @relation("WarehouseMemberships", fields: [userId], references: [id], onDelete: Cascade)
  warehouse    Warehouse @relation(fields: [warehouseId], references: [id], onDelete: Cascade)

  @@unique([userId, warehouseId])
  @@index([warehouseId])
  @@index([assignedById])
}

model ShippingRate {
  id            Int        @id @default(autoincrement())
  country       String     @default("BD")
  area          String
  baseCost      Decimal    @db.Decimal(10, 2)
  weightSlabs   Json?
  freeMinOrder  Decimal?   @db.Decimal(10, 2)
  isActive      Boolean    @default(true)
  priority      Int        @default(1000)
  createdAt     DateTime   @default(now())
  updatedAt     DateTime   @updatedAt
  district      String?
  courierId     Int?
  deliveryType  String?
  estimatedDays Int?
  warehouseId   Int?
  shipments     Shipment[]
  courier       Courier?   @relation(fields: [courierId], references: [id])
  warehouse     Warehouse? @relation(fields: [warehouseId], references: [id])

  @@index([country, area])
  @@index([isActive, priority])
  @@index([warehouseId])
  @@index([courierId])
}

model StockLevel {
  id               Int                    @id @default(autoincrement())
  warehouseId      Int
  productVariantId Int
  quantity         Int                    @default(0)
  reserved         Int                    @default(0)
  createdAt        DateTime               @default(now())
  updatedAt        DateTime               @updatedAt
  reservations     InventoryReservation[]
  variant          ProductVariant         @relation(fields: [productVariantId], references: [id])
  warehouse        Warehouse              @relation(fields: [warehouseId], references: [id])

  @@unique([warehouseId, productVariantId])
}

model InventoryReservation {
  id           Int        @id @default(autoincrement())
  stockLevelId Int
  orderId      Int?
  userId       String?
  quantity     Int
  reason       String?
  expiresAt    DateTime?
  createdAt    DateTime   @default(now())
  order        Order?     @relation(fields: [orderId], references: [id])
  stockLevel   StockLevel @relation(fields: [stockLevelId], references: [id])
  user         User?      @relation(fields: [userId], references: [id])
}

model InventoryDailySnapshot {
  id                Int            @id @default(autoincrement())
  snapshotDate      DateTime       @db.Date
  productId         Int
  variantId         Int
  stock             Int            @default(0)
  lowStockThreshold Int            @default(10)
  status            String         @db.VarChar(20)
  createdAt         DateTime       @default(now())
  updatedAt         DateTime       @updatedAt
  warehouseId       Int?
  product           Product        @relation(fields: [productId], references: [id])
  variant           ProductVariant @relation(fields: [variantId], references: [id])
  warehouse         Warehouse?     @relation(fields: [warehouseId], references: [id])

  @@unique([snapshotDate, variantId])
  @@index([snapshotDate])
}

model InventoryWarehouseDailySnapshot {
  id           Int            @id @default(autoincrement())
  snapshotDate DateTime       @db.Date
  productId    Int
  variantId    Int
  warehouseId  Int
  quantity     Int            @default(0)
  reserved     Int            @default(0)
  available    Int            @default(0)
  createdAt    DateTime       @default(now())
  updatedAt    DateTime       @updatedAt
  product      Product        @relation(fields: [productId], references: [id])
  variant      ProductVariant @relation(fields: [variantId], references: [id])
  warehouse    Warehouse      @relation(fields: [warehouseId], references: [id])

  @@unique([snapshotDate, variantId, warehouseId])
  @@index([snapshotDate, warehouseId])
}

model ShipmentItem {
  id          Int       @id @default(autoincrement())
  shipmentId  Int
  orderItemId Int
  quantity    Int       @default(1)
  createdAt   DateTime  @default(now())
  orderItem   OrderItem @relation(fields: [orderItemId], references: [id])
  shipment    Shipment  @relation(fields: [shipmentId], references: [id])
}

model ShipmentStatusLog {
  id         Int             @id @default(autoincrement())
  shipmentId Int
  fromStatus ShipmentStatus?
  toStatus   ShipmentStatus
  source     String          @default("SYSTEM")
  note       String?
  createdAt  DateTime        @default(now())
  shipment   Shipment        @relation(fields: [shipmentId], references: [id], onDelete: Cascade)

  @@index([shipmentId, createdAt])
  @@index([toStatus, createdAt])
}

model ShipmentCostLog {
  id          Int      @id @default(autoincrement())
  shipmentId  Int
  costType    String
  amount      Decimal  @db.Decimal(10, 2)
  note        String?
  createdById String?
  createdAt   DateTime @default(now())
  createdBy   User?    @relation(fields: [createdById], references: [id])
  shipment    Shipment @relation(fields: [shipmentId], references: [id], onDelete: Cascade)

  @@index([shipmentId, createdAt])
  @@index([createdById])
}

model ShipmentAssignment {
  id           Int        @id @default(autoincrement())
  shipmentId   Int
  assignedToId String
  assignedById String?
  warehouseId  Int?
  role         String?
  note         String?
  assignedAt   DateTime   @default(now())
  completedAt  DateTime?
  assignedBy   User?      @relation("ShipmentAssignmentBy", fields: [assignedById], references: [id])
  assignedTo   User       @relation("ShipmentAssignmentTo", fields: [assignedToId], references: [id], onDelete: Cascade)
  shipment     Shipment   @relation(fields: [shipmentId], references: [id], onDelete: Cascade)
  warehouse    Warehouse? @relation(fields: [warehouseId], references: [id])

  @@index([shipmentId])
  @@index([assignedToId])
  @@index([warehouseId])
}

model PayrollProfile {
  id              Int            @id @default(autoincrement())
  userId          String         @unique
  warehouseId     Int?
  employeeCode    String?        @unique
  paymentType     String         @default("MONTHLY")
  baseSalary      Decimal        @db.Decimal(10, 2)
  bankName        String?
  bankAccountNo   String?
  accountHolder   String?
  mobileBankingNo String?
  paymentMethod   String?
  joiningDate     DateTime?
  isActive        Boolean        @default(true)
  notes           String?
  createdAt       DateTime       @default(now())
  updatedAt       DateTime       @updatedAt
  entries         PayrollEntry[]
  user            User           @relation(fields: [userId], references: [id], onDelete: Cascade)
  warehouse       Warehouse?     @relation(fields: [warehouseId], references: [id])

  @@index([warehouseId])
}

model PayrollPeriod {
  id        Int            @id @default(autoincrement())
  name      String
  startDate DateTime
  endDate   DateTime
  status    String         @default("OPEN")
  notes     String?
  createdAt DateTime       @default(now())
  updatedAt DateTime       @updatedAt
  entries   PayrollEntry[]

  @@unique([startDate, endDate])
}

model PayrollEntry {
  id               Int                 @id @default(autoincrement())
  payrollPeriodId  Int
  payrollProfileId Int
  userId           String
  warehouseId      Int?
  basicAmount      Decimal             @db.Decimal(10, 2)
  overtimeAmount   Decimal             @default(0) @db.Decimal(10, 2)
  bonusAmount      Decimal             @default(0) @db.Decimal(10, 2)
  deductionAmount  Decimal             @default(0) @db.Decimal(10, 2)
  netAmount        Decimal             @db.Decimal(10, 2)
  paymentStatus    String              @default("PENDING")
  paidAt           DateTime?
  note             String?
  createdAt        DateTime            @default(now())
  updatedAt        DateTime            @updatedAt
  adjustments      PayrollAdjustment[]
  payrollPeriod    PayrollPeriod       @relation(fields: [payrollPeriodId], references: [id], onDelete: Cascade)
  payrollProfile   PayrollProfile      @relation(fields: [payrollProfileId], references: [id], onDelete: Cascade)
  user             User                @relation(fields: [userId], references: [id], onDelete: Cascade)
  warehouse        Warehouse?          @relation(fields: [warehouseId], references: [id])

  @@unique([payrollPeriodId, payrollProfileId])
  @@index([payrollPeriodId])
  @@index([userId])
  @@index([warehouseId])
}

model PayrollAdjustment {
  id             Int          @id @default(autoincrement())
  payrollEntryId Int
  type           String
  amount         Decimal      @db.Decimal(10, 2)
  reason         String?
  createdAt      DateTime     @default(now())
  payrollEntry   PayrollEntry @relation(fields: [payrollEntryId], references: [id], onDelete: Cascade)

  @@index([payrollEntryId])
}

model Refund {
  id          Int          @id @default(autoincrement())
  orderId     Int
  userId      String?
  paymentId   Int?
  orderItemId Int?
  reason      String
  status      RefundStatus @default(REQUESTED)
  amount      Decimal      @db.Decimal(10, 2)
  quantity    Int?
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
  order       Order        @relation(fields: [orderId], references: [id])
  orderItem   OrderItem?   @relation(fields: [orderItemId], references: [id])
  payment     Payment?     @relation(fields: [paymentId], references: [id])
  user        User?        @relation(fields: [userId], references: [id])

  @@index([userId])
}

model InventoryLog {
  id          Int             @id @default(autoincrement())
  productId   Int
  variantId   Int?
  warehouseId Int?
  change      Int
  reason      String
  createdAt   DateTime        @default(now())
  product     Product         @relation(fields: [productId], references: [id])
  variant     ProductVariant? @relation(fields: [variantId], references: [id])
  warehouse   Warehouse?      @relation(fields: [warehouseId], references: [id])
}

model Supplier {
  id                        Int                            @id @default(autoincrement())
  code                      String                         @unique
  name                      String
  companyType               SupplierCompanyType            @default(PROPRIETOR)
  contactName               String?
  email                     String?
  phone                     String?
  address                   String?
  city                      String?
  country                   String?                        @default("BD")
  leadTimeDays              Int?
  paymentTermsDays          Int?
  currency                  String                         @default("BDT") @db.VarChar(3)
  taxNumber                 String?
  notes                     String?
  isActive                  Boolean                        @default(true)
  createdAt                 DateTime                       @default(now())
  updatedAt                 DateTime                       @updatedAt
  documents                 SupplierDocument[]
  purchaseOrders            PurchaseOrder[]
  supplierReturns           SupplierReturn[]
  invoices                  SupplierInvoice[]
  payments                  SupplierPayment[]
  paymentRequests           PaymentRequest[]
  ledgerEntries             SupplierLedgerEntry[]
  rfqInvites                RfqSupplierInvite[]
  quotations                SupplierQuotation[]
  comparativeStatementLines ComparativeStatementLine[]
  rfqAwards                 RfqAward[]
  categories                SupplierCategorySupplier[]
  rfqNotifications          RfqNotification[]
  slaPolicies               SupplierSlaPolicy[]
  slaBreaches               SupplierSlaBreach[]
  slaTerminationCases       SupplierSlaTerminationCase[]
  portalAccesses            SupplierPortalAccess[]
  profileUpdateRequests     SupplierProfileUpdateRequest[]
  feedbacks                 SupplierFeedback[]
  portalNotifications       SupplierPortalNotification[]

  @@index([companyType])
  @@index([name])
  @@index([isActive])
}

model SupplierDocument {
  id                 Int                                @id @default(autoincrement())
  supplierId         Int
  type               SupplierDocumentType
  documentNumber     String?
  fileUrl            String
  fileName           String?
  mimeType           String?
  fileSize           Int?
  issuedAt           DateTime?
  expiresAt          DateTime?
  verificationStatus SupplierDocumentVerificationStatus @default(PENDING)
  verifiedAt         DateTime?
  verifiedById       String?
  verificationNote   String?
  lastReminderAt     DateTime?
  createdAt          DateTime                           @default(now())
  updatedAt          DateTime                           @updatedAt
  supplier           Supplier                           @relation(fields: [supplierId], references: [id], onDelete: Cascade)
  verifiedBy         User?                              @relation("SupplierDocumentsVerifiedBy", fields: [verifiedById], references: [id], onDelete: SetNull)

  @@unique([supplierId, type])
  @@index([supplierId])
  @@index([type])
  @@index([expiresAt])
  @@index([verificationStatus])
  @@index([verifiedById])
}

model PurchaseRequisition {
  id                           Int                                @id @default(autoincrement())
  requisitionNumber            String                             @unique
  warehouseId                  Int
  status                       PurchaseRequisitionStatus          @default(DRAFT)
  title                        String?
  purpose                      String?
  budgetCode                   String?
  boqReference                 String?
  specification                String?
  planningNote                 String?
  estimatedAmount              Decimal?                           @db.Decimal(14, 2)
  endorsementRequiredCount     Int                                @default(1)
  requestedAt                  DateTime                           @default(now())
  neededBy                     DateTime?
  submittedAt                  DateTime?
  budgetClearedAt              DateTime?
  endorsedAt                   DateTime?
  approvedAt                   DateTime?
  rejectedAt                   DateTime?
  convertedAt                  DateTime?
  routedToProcurementAt        DateTime?
  createdById                  String?
  budgetClearedById            String?
  endorsedById                 String?
  approvedById                 String?
  convertedById                String?
  assignedProcurementOfficerId String?
  note                         String?
  createdAt                    DateTime                           @default(now())
  updatedAt                    DateTime                           @updatedAt
  approvedBy                   User?                              @relation("PurchaseRequisitionsApproved", fields: [approvedById], references: [id], onDelete: SetNull)
  budgetClearedBy              User?                              @relation("PurchaseRequisitionsBudgetCleared", fields: [budgetClearedById], references: [id], onDelete: SetNull)
  endorsedBy                   User?                              @relation("PurchaseRequisitionsEndorsed", fields: [endorsedById], references: [id], onDelete: SetNull)
  assignedProcurementOfficer   User?                              @relation("PurchaseRequisitionsAssignedProcurement", fields: [assignedProcurementOfficerId], references: [id], onDelete: SetNull)
  convertedBy                  User?                              @relation("PurchaseRequisitionsConverted", fields: [convertedById], references: [id], onDelete: SetNull)
  createdBy                    User?                              @relation("PurchaseRequisitionsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  items                        PurchaseRequisitionItem[]
  attachments                  PurchaseRequisitionAttachment[]
  approvalEvents               PurchaseRequisitionApprovalEvent[]
  versions                     PurchaseRequisitionVersion[]
  notifications                PurchaseRequisitionNotification[]
  rfqs                         Rfq[]
  purchaseOrders               PurchaseOrder[]
  warehouse                    Warehouse                          @relation(fields: [warehouseId], references: [id], onDelete: Restrict)

  @@index([warehouseId, status])
  @@index([createdById])
  @@index([approvedById])
  @@index([budgetClearedById])
  @@index([endorsedById])
  @@index([assignedProcurementOfficerId])
  @@index([requestedAt])
}

model PurchaseRequisitionItem {
  id                    Int                 @id @default(autoincrement())
  purchaseRequisitionId Int
  productVariantId      Int
  description           String?
  quantityRequested     Int
  quantityApproved      Int?
  createdAt             DateTime            @default(now())
  updatedAt             DateTime            @updatedAt
  productVariant        ProductVariant      @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  purchaseRequisition   PurchaseRequisition @relation(fields: [purchaseRequisitionId], references: [id], onDelete: Cascade)

  @@index([purchaseRequisitionId])
  @@index([productVariantId])
}

model PurchaseRequisitionAttachment {
  id                    Int                 @id @default(autoincrement())
  purchaseRequisitionId Int
  fileUrl               String
  fileName              String
  mimeType              String?
  fileSize              Int?
  note                  String?
  uploadedById          String?
  createdAt             DateTime            @default(now())
  purchaseRequisition   PurchaseRequisition @relation(fields: [purchaseRequisitionId], references: [id], onDelete: Cascade)
  uploadedBy            User?               @relation("PurchaseRequisitionAttachmentsUploaded", fields: [uploadedById], references: [id], onDelete: SetNull)

  @@index([purchaseRequisitionId])
  @@index([uploadedById])
}

model PurchaseRequisitionApprovalEvent {
  id                    Int                                 @id @default(autoincrement())
  purchaseRequisitionId Int
  stage                 PurchaseRequisitionApprovalStage
  decision              PurchaseRequisitionApprovalDecision
  note                  String?
  actedById             String?
  actedAt               DateTime                            @default(now())
  purchaseRequisition   PurchaseRequisition                 @relation(fields: [purchaseRequisitionId], references: [id], onDelete: Cascade)
  actedBy               User?                               @relation("PurchaseRequisitionApprovalEvents", fields: [actedById], references: [id], onDelete: SetNull)

  @@unique([purchaseRequisitionId, stage, actedById])
  @@index([purchaseRequisitionId, stage])
  @@index([actedById])
}

model PurchaseRequisitionVersion {
  id                    Int                              @id @default(autoincrement())
  purchaseRequisitionId Int
  versionNo             Int
  stage                 PurchaseRequisitionApprovalStage
  action                String
  snapshot              Json
  createdById           String?
  createdAt             DateTime                         @default(now())
  purchaseRequisition   PurchaseRequisition              @relation(fields: [purchaseRequisitionId], references: [id], onDelete: Cascade)
  createdBy             User?                            @relation("PurchaseRequisitionVersionsCreated", fields: [createdById], references: [id], onDelete: SetNull)

  @@unique([purchaseRequisitionId, versionNo])
  @@index([purchaseRequisitionId, createdAt])
  @@index([createdById])
}

model PurchaseRequisitionNotification {
  id                    Int                              @id @default(autoincrement())
  purchaseRequisitionId Int
  stage                 PurchaseRequisitionApprovalStage
  channel               WorkflowNotificationChannel
  status                WorkflowNotificationStatus       @default(PENDING)
  recipientUserId       String?
  recipientEmail        String?
  message               String
  sentAt                DateTime?
  readAt                DateTime?
  createdAt             DateTime                         @default(now())
  metadata              Json?
  purchaseRequisition   PurchaseRequisition              @relation(fields: [purchaseRequisitionId], references: [id], onDelete: Cascade)
  recipientUser         User?                            @relation("PurchaseRequisitionNotificationsReceived", fields: [recipientUserId], references: [id], onDelete: SetNull)

  @@index([purchaseRequisitionId, stage, createdAt])
  @@index([recipientUserId])
  @@index([recipientUserId, readAt])
  @@index([status, channel])
}

model WarehouseZone {
  id          Int              @id @default(autoincrement())
  warehouseId Int
  code        String
  name        String
  description String?
  isActive    Boolean          @default(true)
  createdAt   DateTime         @default(now())
  updatedAt   DateTime         @updatedAt
  warehouse   Warehouse        @relation(fields: [warehouseId], references: [id], onDelete: Cascade)
  aisles      WarehouseAisle[]
  bins        WarehouseBin[]

  @@unique([warehouseId, code])
  @@index([warehouseId, isActive])
}

model WarehouseAisle {
  id          Int            @id @default(autoincrement())
  warehouseId Int
  zoneId      Int
  code        String
  name        String
  description String?
  isActive    Boolean        @default(true)
  createdAt   DateTime       @default(now())
  updatedAt   DateTime       @updatedAt
  warehouse   Warehouse      @relation(fields: [warehouseId], references: [id], onDelete: Cascade)
  zone        WarehouseZone  @relation(fields: [zoneId], references: [id], onDelete: Cascade)
  bins        WarehouseBin[]

  @@unique([zoneId, code])
  @@index([warehouseId, zoneId])
  @@index([isActive])
}

model WarehouseBin {
  id                Int                         @id @default(autoincrement())
  warehouseId       Int
  zoneId            Int
  aisleId           Int
  code              String
  name              String
  description       String?
  isActive          Boolean                     @default(true)
  createdAt         DateTime                    @default(now())
  updatedAt         DateTime                    @updatedAt
  warehouse         Warehouse                   @relation(fields: [warehouseId], references: [id], onDelete: Cascade)
  zone              WarehouseZone               @relation(fields: [zoneId], references: [id], onDelete: Cascade)
  aisle             WarehouseAisle              @relation(fields: [aisleId], references: [id], onDelete: Cascade)
  stockLevels       StockBinLevel[]
  verificationLines InventoryVerificationLine[]

  @@unique([aisleId, code])
  @@index([warehouseId, zoneId, aisleId])
  @@index([isActive])
}

model StockBinLevel {
  id               Int            @id @default(autoincrement())
  warehouseId      Int
  binId            Int
  productVariantId Int
  quantity         Int            @default(0)
  reserved         Int            @default(0)
  createdAt        DateTime       @default(now())
  updatedAt        DateTime       @updatedAt
  warehouse        Warehouse      @relation(fields: [warehouseId], references: [id], onDelete: Cascade)
  bin              WarehouseBin   @relation(fields: [binId], references: [id], onDelete: Cascade)
  variant          ProductVariant @relation(fields: [productVariantId], references: [id], onDelete: Cascade)

  @@unique([binId, productVariantId])
  @@index([warehouseId, productVariantId])
}

model InventoryVerification {
  id               Int                                    @id @default(autoincrement())
  warehouseId      Int
  frequency        InventoryVerificationFrequency
  periodStart      DateTime
  periodEnd        DateTime
  status           InventoryVerificationStatus            @default(DRAFT)
  note             String?
  submittedAt      DateTime?
  approvedAt       DateTime?
  rejectedAt       DateTime?
  createdById      String?
  approvedById     String?
  createdAt        DateTime                               @default(now())
  updatedAt        DateTime                               @updatedAt
  warehouse        Warehouse                              @relation(fields: [warehouseId], references: [id], onDelete: Cascade)
  createdBy        User?                                  @relation("InventoryVerificationsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  approvedBy       User?                                  @relation("InventoryVerificationsApproved", fields: [approvedById], references: [id], onDelete: SetNull)
  committeeMembers InventoryVerificationCommitteeMember[]
  lines            InventoryVerificationLine[]
  approvalEvents   InventoryVerificationApprovalEvent[]

  @@index([warehouseId, periodStart, periodEnd])
  @@index([status])
  @@index([createdById])
}

model InventoryVerificationCommitteeMember {
  id             Int                   @id @default(autoincrement())
  verificationId Int
  userId         String
  role           String?
  assignedAt     DateTime              @default(now())
  verification   InventoryVerification @relation(fields: [verificationId], references: [id], onDelete: Cascade)
  user           User                  @relation("InventoryVerificationCommitteeMemberships", fields: [userId], references: [id], onDelete: Cascade)

  @@unique([verificationId, userId])
  @@index([userId])
}

model InventoryVerificationLine {
  id               Int                   @id @default(autoincrement())
  verificationId   Int
  productVariantId Int
  binId            Int?
  systemQty        Int
  countedQty       Int
  variance         Int
  note             String?
  verification     InventoryVerification @relation(fields: [verificationId], references: [id], onDelete: Cascade)
  productVariant   ProductVariant        @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  bin              WarehouseBin?         @relation(fields: [binId], references: [id], onDelete: SetNull)

  @@index([verificationId])
  @@index([productVariantId])
  @@index([binId])
}

model InventoryVerificationApprovalEvent {
  id             Int                                   @id @default(autoincrement())
  verificationId Int
  stage          InventoryVerificationApprovalStage
  decision       InventoryVerificationApprovalDecision
  note           String?
  actedById      String?
  actedAt        DateTime                              @default(now())
  verification   InventoryVerification                 @relation(fields: [verificationId], references: [id], onDelete: Cascade)
  actedBy        User?                                 @relation("InventoryVerificationApprovalEvents", fields: [actedById], references: [id], onDelete: SetNull)

  @@index([verificationId, stage])
  @@index([actedById])
}

model ReorderAlert {
  id               Int                @id @default(autoincrement())
  warehouseId      Int
  productVariantId Int
  stockOnHand      Int
  threshold        Int
  suggestedQty     Int
  status           ReorderAlertStatus @default(OPEN)
  note             String?
  createdById      String?
  createdAt        DateTime           @default(now())
  resolvedAt       DateTime?
  warehouse        Warehouse          @relation(fields: [warehouseId], references: [id], onDelete: Cascade)
  productVariant   ProductVariant     @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  createdBy        User?              @relation("ReorderAlertsCreated", fields: [createdById], references: [id], onDelete: SetNull)

  @@index([warehouseId, status])
  @@index([productVariantId, status])
  @@index([createdAt])
}

model MaterialRequest {
  id                         Int                            @id @default(autoincrement())
  requestNumber              String                         @unique
  warehouseId                Int
  status                     MaterialRequestStatus          @default(DRAFT)
  title                      String?
  purpose                    String?
  budgetCode                 String?
  boqReference               String?
  specification              String?
  note                       String?
  requestedAt                DateTime                       @default(now())
  requiredBy                 DateTime?
  submittedAt                DateTime?
  supervisorEndorsedAt       DateTime?
  supervisorEndorsedById     String?
  projectManagerEndorsedAt   DateTime?
  projectManagerEndorsedById String?
  adminApprovedAt            DateTime?
  adminApprovedById          String?
  rejectedAt                 DateTime?
  rejectedById               String?
  createdById                String?
  createdAt                  DateTime                       @default(now())
  updatedAt                  DateTime                       @updatedAt
  warehouse                  Warehouse                      @relation(fields: [warehouseId], references: [id], onDelete: Restrict)
  createdBy                  User?                          @relation("MaterialRequestsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  supervisorEndorsedBy       User?                          @relation("MaterialRequestsSupervisorEndorsed", fields: [supervisorEndorsedById], references: [id], onDelete: SetNull)
  projectManagerEndorsedBy   User?                          @relation("MaterialRequestsProjectManagerEndorsed", fields: [projectManagerEndorsedById], references: [id], onDelete: SetNull)
  adminApprovedBy            User?                          @relation("MaterialRequestsAdminApproved", fields: [adminApprovedById], references: [id], onDelete: SetNull)
  rejectedBy                 User?                          @relation("MaterialRequestsRejected", fields: [rejectedById], references: [id], onDelete: SetNull)
  items                      MaterialRequestItem[]
  attachments                MaterialRequestAttachment[]
  approvalEvents             MaterialRequestApprovalEvent[]
  releaseNotes               MaterialReleaseNote[]
  assetRegisters             AssetRegister[]

  @@index([warehouseId, status])
  @@index([createdById, requestedAt])
  @@index([adminApprovedById])
  @@index([requestedAt])
}

model MaterialRequestItem {
  id                Int                       @id @default(autoincrement())
  materialRequestId Int
  productVariantId  Int
  description       String?
  quantityRequested Int
  quantityReleased  Int                       @default(0)
  createdAt         DateTime                  @default(now())
  updatedAt         DateTime                  @updatedAt
  materialRequest   MaterialRequest           @relation(fields: [materialRequestId], references: [id], onDelete: Cascade)
  productVariant    ProductVariant            @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  releaseItems      MaterialReleaseNoteItem[]

  @@index([materialRequestId])
  @@index([productVariantId])
}

model MaterialRequestAttachment {
  id                Int             @id @default(autoincrement())
  materialRequestId Int
  fileUrl           String
  fileName          String
  mimeType          String?
  fileSize          Int?
  note              String?
  uploadedById      String?
  createdAt         DateTime        @default(now())
  materialRequest   MaterialRequest @relation(fields: [materialRequestId], references: [id], onDelete: Cascade)
  uploadedBy        User?           @relation("MaterialRequestAttachmentsUploaded", fields: [uploadedById], references: [id], onDelete: SetNull)

  @@index([materialRequestId, createdAt])
  @@index([uploadedById])
}

model MaterialRequestApprovalEvent {
  id                Int                             @id @default(autoincrement())
  materialRequestId Int
  stage             MaterialRequestApprovalStage
  decision          MaterialRequestApprovalDecision
  note              String?
  actedById         String?
  actedAt           DateTime                        @default(now())
  materialRequest   MaterialRequest                 @relation(fields: [materialRequestId], references: [id], onDelete: Cascade)
  actedBy           User?                           @relation("MaterialRequestApprovalEvents", fields: [actedById], references: [id], onDelete: SetNull)

  @@index([materialRequestId, stage, actedAt])
  @@index([actedById, actedAt])
}

model MaterialReleaseNote {
  id                Int                       @id @default(autoincrement())
  releaseNumber     String                    @unique
  challanNumber     String?                   @unique
  waybillNumber     String?                   @unique
  materialRequestId Int
  warehouseId       Int
  status            MaterialReleaseStatus     @default(ISSUED)
  note              String?
  releasedAt        DateTime                  @default(now())
  releasedById      String?
  createdAt         DateTime                  @default(now())
  updatedAt         DateTime                  @updatedAt
  materialRequest   MaterialRequest           @relation(fields: [materialRequestId], references: [id], onDelete: Restrict)
  warehouse         Warehouse                 @relation(fields: [warehouseId], references: [id], onDelete: Restrict)
  releasedBy        User?                     @relation("MaterialReleasesIssued", fields: [releasedById], references: [id], onDelete: SetNull)
  items             MaterialReleaseNoteItem[]
  assetRegisters    AssetRegister[]

  @@index([warehouseId, releasedAt])
  @@index([materialRequestId])
  @@index([releasedById])
}

model MaterialReleaseNoteItem {
  id                    Int                 @id @default(autoincrement())
  materialReleaseNoteId Int
  materialRequestItemId Int
  productVariantId      Int
  quantityReleased      Int
  unitCost              Decimal?            @db.Decimal(12, 2)
  createdAt             DateTime            @default(now())
  materialReleaseNote   MaterialReleaseNote @relation(fields: [materialReleaseNoteId], references: [id], onDelete: Cascade)
  materialRequestItem   MaterialRequestItem @relation(fields: [materialRequestItemId], references: [id], onDelete: Restrict)
  productVariant        ProductVariant      @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  assetRegisters        AssetRegister[]

  @@index([materialReleaseNoteId])
  @@index([materialRequestItemId])
  @@index([productVariantId])
}

model AssetRegister {
  id                    Int                      @id @default(autoincrement())
  assetTag              String                   @unique
  warehouseId           Int
  productVariantId      Int
  materialRequestId     Int?
  materialReleaseNoteId Int?
  materialReleaseItemId Int?
  status                AssetRegisterStatus      @default(ACTIVE)
  assignedTo            String?
  note                  String?
  acquiredAt            DateTime                 @default(now())
  createdById           String?
  createdAt             DateTime                 @default(now())
  updatedAt             DateTime                 @updatedAt
  warehouse             Warehouse                @relation(fields: [warehouseId], references: [id], onDelete: Restrict)
  productVariant        ProductVariant           @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  materialRequest       MaterialRequest?         @relation(fields: [materialRequestId], references: [id], onDelete: SetNull)
  materialReleaseNote   MaterialReleaseNote?     @relation(fields: [materialReleaseNoteId], references: [id], onDelete: SetNull)
  materialReleaseItem   MaterialReleaseNoteItem? @relation(fields: [materialReleaseItemId], references: [id], onDelete: SetNull)
  createdBy             User?                    @relation("AssetRegistersCreated", fields: [createdById], references: [id], onDelete: SetNull)

  @@index([warehouseId, status])
  @@index([productVariantId, status])
  @@index([materialRequestId])
  @@index([materialReleaseNoteId])
  @@index([materialReleaseItemId])
  @@index([createdById])
}

model SupplierPortalAccess {
  id                      String                     @id @default(cuid())
  userId                  String                     @unique
  supplierId              Int
  status                  SupplierPortalAccessStatus @default(ACTIVE)
  twoFactorRequired       Boolean                    @default(false)
  twoFactorMethod         String?
  twoFactorLastVerifiedAt DateTime?
  note                    String?
  createdAt               DateTime                   @default(now())
  updatedAt               DateTime                   @updatedAt
  createdById             String?
  createdBy               User?                      @relation("SupplierPortalAccessCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)
  supplier                Supplier                   @relation(fields: [supplierId], references: [id], onDelete: Cascade)
  user                    User                       @relation("SupplierPortalAccessUser", fields: [userId], references: [id], onDelete: Cascade)

  @@index([supplierId, status])
  @@index([createdById])
}

model SupplierProfileUpdateRequest {
  id                Int                                @id @default(autoincrement())
  supplierId        Int
  requestedByUserId String
  requestType       SupplierProfileUpdateRequestType   @default(PROFILE_UPDATE)
  status            SupplierProfileUpdateRequestStatus @default(PENDING)
  payload           Json
  note              String?
  reviewNote        String?
  requestedAt       DateTime                           @default(now())
  reviewedAt        DateTime?
  reviewedById      String?
  createdAt         DateTime                           @default(now())
  updatedAt         DateTime                           @updatedAt
  requestedBy       User                               @relation("SupplierProfileUpdateRequestsSubmitted", fields: [requestedByUserId], references: [id], onDelete: Cascade)
  reviewedBy        User?                              @relation("SupplierProfileUpdateRequestsReviewed", fields: [reviewedById], references: [id], onDelete: SetNull)
  supplier          Supplier                           @relation(fields: [supplierId], references: [id], onDelete: Cascade)

  @@index([supplierId, status])
  @@index([requestedByUserId, createdAt])
  @@index([reviewedById])
}

model SupplierFeedback {
  id                   Int                        @id @default(autoincrement())
  supplierId           Int
  sourceType           SupplierFeedbackSourceType @default(INTERNAL)
  sourceReference      String?
  clientName           String?
  clientEmail          String?
  rating               Int
  serviceQualityRating Int?
  deliveryRating       Int?
  complianceRating     Int?
  comment              String?
  createdById          String?
  createdAt            DateTime                   @default(now())
  updatedAt            DateTime                   @updatedAt
  createdBy            User?                      @relation("SupplierFeedbackCreated", fields: [createdById], references: [id], onDelete: SetNull)
  supplier             Supplier                   @relation(fields: [supplierId], references: [id], onDelete: Cascade)

  @@index([supplierId, createdAt])
  @@index([sourceType, createdAt])
  @@index([createdById])
}

model SupplierPortalNotification {
  id             Int                            @id @default(autoincrement())
  supplierId     Int
  userId         String?
  channel        WorkflowNotificationChannel
  status         WorkflowNotificationStatus     @default(PENDING)
  type           SupplierPortalNotificationType @default(GENERAL)
  title          String
  message        String
  recipientEmail String?
  metadata       Json?
  sentAt         DateTime?
  readAt         DateTime?
  createdById    String?
  createdAt      DateTime                       @default(now())
  createdBy      User?                          @relation("SupplierPortalNotificationsCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)
  supplier       Supplier                       @relation(fields: [supplierId], references: [id], onDelete: Cascade)
  user           User?                          @relation("SupplierPortalNotificationsUser", fields: [userId], references: [id], onDelete: SetNull)

  @@index([supplierId, createdAt])
  @@index([userId, createdAt])
  @@index([status, channel])
  @@index([type, createdAt])
  @@index([createdById])
}

model InvestorPortalAccess {
  id          String                     @id @default(cuid())
  userId      String                     @unique
  investorId  Int
  status      InvestorPortalAccessStatus @default(ACTIVE)
  note        String?
  createdAt   DateTime                   @default(now())
  updatedAt   DateTime                   @updatedAt
  createdById String?
  createdBy   User?                      @relation("InvestorPortalAccessCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)
  investor    Investor                   @relation(fields: [investorId], references: [id], onDelete: Cascade)
  user        User                       @relation("InvestorPortalAccessUser", fields: [userId], references: [id], onDelete: Cascade)

  @@index([investorId, status])
  @@index([createdById])
}

model Investor {
  id                          Int                            @id @default(autoincrement())
  code                        String                         @unique
  name                        String
  legalName                   String?
  email                       String?
  phone                       String?
  taxNumber                   String?
  nationalIdNumber            String?
  passportNumber              String?
  bankName                    String?
  bankAccountName             String?
  bankAccountNumber           String?
  beneficiaryVerifiedAt       DateTime?
  beneficiaryVerifiedById     String?
  beneficiaryVerificationNote String?
  status                      InvestorStatus                 @default(ACTIVE)
  kycStatus                   InvestorKycStatus              @default(PENDING)
  kycVerifiedAt               DateTime?
  kycReference                String?
  notes                       String?
  createdById                 String?
  createdAt                   DateTime                       @default(now())
  updatedAt                   DateTime                       @updatedAt
  createdBy                   User?                          @relation("InvestorsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  transactions                InvestorCapitalTransaction[]
  allocations                 InvestorProductAllocation[]
  profitRunAllocations        InvestorProfitRunAllocation[]
  payouts                     InvestorProfitPayout[]
  portalAccesses              InvestorPortalAccess[]
  documents                   InvestorDocument[]
  changeRequests              InvestorMasterChangeRequest[]
  profileUpdateRequests       InvestorProfileUpdateRequest[]
  withdrawalRequests          InvestorWithdrawalRequest[]
  portalNotifications         InvestorPortalNotification[]
  statementSchedules          InvestorStatementSchedule[]
  beneficiaryVerifiedBy       User?                          @relation("InvestorBeneficiaryVerifiedBy", fields: [beneficiaryVerifiedById], references: [id], onDelete: SetNull)

  @@index([name])
  @@index([status])
  @@index([kycStatus])
  @@index([createdById])
  @@index([beneficiaryVerifiedById])
}

model InvestorCapitalTransaction {
  id                Int                        @id @default(autoincrement())
  transactionNumber String                     @unique
  investorId        Int
  transactionDate   DateTime                   @default(now())
  type              InvestorTransactionType
  direction         InvestorLedgerDirection
  amount            Decimal                    @db.Decimal(14, 2)
  currency          String                     @default("BDT") @db.VarChar(3)
  note              String?
  referenceType     String?
  referenceNumber   String?
  productVariantId  Int?
  createdById       String?
  createdAt         DateTime                   @default(now())
  updatedAt         DateTime                   @updatedAt
  createdBy         User?                      @relation("InvestorCapitalTransactionsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  investor          Investor                   @relation(fields: [investorId], references: [id], onDelete: Cascade)
  productVariant    ProductVariant?            @relation(fields: [productVariantId], references: [id], onDelete: SetNull)
  payout            InvestorProfitPayout?
  withdrawalRequest InvestorWithdrawalRequest?

  @@index([investorId, transactionDate])
  @@index([type])
  @@index([direction])
  @@index([productVariantId])
  @@index([createdById])
}

model InvestorProductAllocation {
  id                   Int                           @id @default(autoincrement())
  investorId           Int
  productVariantId     Int
  effectiveFrom        DateTime                      @default(now())
  effectiveTo          DateTime?
  participationPercent Decimal?                      @db.Decimal(5, 2)
  committedAmount      Decimal?                      @db.Decimal(14, 2)
  status               String                        @default("ACTIVE")
  note                 String?
  createdById          String?
  createdAt            DateTime                      @default(now())
  updatedAt            DateTime                      @updatedAt
  createdBy            User?                         @relation("InvestorProductAllocationsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  investor             Investor                      @relation(fields: [investorId], references: [id], onDelete: Cascade)
  productVariant       ProductVariant                @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  profitRunAllocations InvestorProfitRunAllocation[] @relation("InvestorProfitRunAllocationSource")

  @@index([investorId, status])
  @@index([productVariantId, status])
  @@index([createdById])
}

model InvestorProfitRun {
  id                    Int                                  @id @default(autoincrement())
  runNumber             String                               @unique
  fromDate              DateTime
  toDate                DateTime
  status                InvestorProfitRunStatus              @default(PENDING_APPROVAL)
  allocationBasis       InvestorProfitExpenseAllocationBasis
  marketingExpense      Decimal                              @default(0) @db.Decimal(14, 2)
  adsExpense            Decimal                              @default(0) @db.Decimal(14, 2)
  logisticsExpense      Decimal                              @default(0) @db.Decimal(14, 2)
  otherExpense          Decimal                              @default(0) @db.Decimal(14, 2)
  totalOperatingExpense Decimal                              @default(0) @db.Decimal(14, 2)
  totalNetRevenue       Decimal                              @default(0) @db.Decimal(14, 2)
  totalNetCogs          Decimal                              @default(0) @db.Decimal(14, 2)
  totalNetProfit        Decimal                              @default(0) @db.Decimal(14, 2)
  note                  String?
  createdById           String?
  approvedById          String?
  approvedAt            DateTime?
  postedById            String?
  postedAt              DateTime?
  postingNote           String?
  createdAt             DateTime                             @default(now())
  updatedAt             DateTime                             @updatedAt
  createdBy             User?                                @relation("InvestorProfitRunsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  approvedBy            User?                                @relation("InvestorProfitRunsApproved", fields: [approvedById], references: [id], onDelete: SetNull)
  postedBy              User?                                @relation("InvestorProfitRunsPosted", fields: [postedById], references: [id], onDelete: SetNull)
  variantLines          InvestorProfitRunVariant[]
  allocationLines       InvestorProfitRunAllocation[]
  payouts               InvestorProfitPayout[]

  @@index([fromDate, toDate])
  @@index([createdAt])
  @@index([createdById])
  @@index([status])
  @@index([approvedById])
  @@index([postedById])
}

model InvestorProfitRunVariant {
  id                  Int                           @id @default(autoincrement())
  runId               Int
  productVariantId    Int
  unitsSold           Int                           @default(0)
  unitsRefunded       Int                           @default(0)
  unitsNet            Int                           @default(0)
  grossRevenue        Decimal                       @default(0) @db.Decimal(14, 2)
  refundAmount        Decimal                       @default(0) @db.Decimal(14, 2)
  netRevenue          Decimal                       @default(0) @db.Decimal(14, 2)
  grossCogs           Decimal                       @default(0) @db.Decimal(14, 2)
  refundCogs          Decimal                       @default(0) @db.Decimal(14, 2)
  netCogs             Decimal                       @default(0) @db.Decimal(14, 2)
  allocatedExpense    Decimal                       @default(0) @db.Decimal(14, 2)
  netProfit           Decimal                       @default(0) @db.Decimal(14, 2)
  unallocatedSharePct Decimal                       @default(0) @db.Decimal(7, 4)
  createdAt           DateTime                      @default(now())
  updatedAt           DateTime                      @updatedAt
  productVariant      ProductVariant                @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  run                 InvestorProfitRun             @relation(fields: [runId], references: [id], onDelete: Cascade)
  allocationLines     InvestorProfitRunAllocation[]

  @@unique([runId, productVariantId])
  @@index([runId, netProfit])
  @@index([productVariantId])
}

model InvestorProfitRunAllocation {
  id                    Int                        @id @default(autoincrement())
  runId                 Int
  variantLineId         Int
  investorId            Int
  productVariantId      Int
  sourceAllocationId    Int?
  participationSharePct Decimal                    @default(0) @db.Decimal(7, 4)
  allocatedRevenue      Decimal                    @default(0) @db.Decimal(14, 2)
  allocatedNetProfit    Decimal                    @default(0) @db.Decimal(14, 2)
  createdAt             DateTime                   @default(now())
  updatedAt             DateTime                   @updatedAt
  investor              Investor                   @relation(fields: [investorId], references: [id], onDelete: Restrict)
  productVariant        ProductVariant             @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  run                   InvestorProfitRun          @relation(fields: [runId], references: [id], onDelete: Cascade)
  sourceAllocation      InvestorProductAllocation? @relation("InvestorProfitRunAllocationSource", fields: [sourceAllocationId], references: [id], onDelete: SetNull)
  variantLine           InvestorProfitRunVariant   @relation(fields: [variantLineId], references: [id], onDelete: Cascade)

  @@index([runId, investorId])
  @@index([runId, productVariantId])
  @@index([sourceAllocationId])
}

model InvestorProfitPayout {
  id                               Int                          @id @default(autoincrement())
  payoutNumber                     String                       @unique
  runId                            Int
  investorId                       Int
  transactionId                    Int?                         @unique
  currency                         String                       @default("BDT") @db.VarChar(3)
  payoutPercent                    Decimal                      @default(100) @db.Decimal(5, 2)
  holdbackPercent                  Decimal                      @default(0) @db.Decimal(5, 2)
  grossProfitAmount                Decimal                      @default(0) @db.Decimal(14, 2)
  holdbackAmount                   Decimal                      @default(0) @db.Decimal(14, 2)
  payoutAmount                     Decimal                      @default(0) @db.Decimal(14, 2)
  beneficiaryNameSnapshot          String?
  beneficiaryBankNameSnapshot      String?
  beneficiaryAccountNumberSnapshot String?
  beneficiaryVerifiedAt            DateTime?
  beneficiaryVerificationNote      String?
  status                           InvestorProfitPayoutStatus   @default(PENDING_APPROVAL)
  approvalNote                     String?
  approvedById                     String?
  approvedAt                       DateTime?
  rejectedById                     String?
  rejectedAt                       DateTime?
  rejectionReason                  String?
  paymentMethod                    InvestorPayoutPaymentMethod?
  bankReference                    String?
  paidById                         String?
  note                             String?
  holdReason                       String?
  heldAt                           DateTime?
  paidAt                           DateTime?
  releasedById                     String?
  releasedAt                       DateTime?
  releaseNote                      String?
  paymentProofUrl                  String?
  paymentProofUploadedAt           DateTime?
  voidedById                       String?
  voidedAt                         DateTime?
  voidReason                       String?
  voidReversalReference            String?
  createdById                      String?
  createdAt                        DateTime                     @default(now())
  updatedAt                        DateTime                     @updatedAt
  createdBy                        User?                        @relation("InvestorProfitPayoutsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  approvedBy                       User?                        @relation("InvestorProfitPayoutsApproved", fields: [approvedById], references: [id], onDelete: SetNull)
  rejectedBy                       User?                        @relation("InvestorProfitPayoutsRejected", fields: [rejectedById], references: [id], onDelete: SetNull)
  paidBy                           User?                        @relation("InvestorProfitPayoutsPaid", fields: [paidById], references: [id], onDelete: SetNull)
  releasedBy                       User?                        @relation("InvestorProfitPayoutsReleased", fields: [releasedById], references: [id], onDelete: SetNull)
  voidedBy                         User?                        @relation("InvestorProfitPayoutsVoided", fields: [voidedById], references: [id], onDelete: SetNull)
  investor                         Investor                     @relation(fields: [investorId], references: [id], onDelete: Restrict)
  run                              InvestorProfitRun            @relation(fields: [runId], references: [id], onDelete: Cascade)
  transaction                      InvestorCapitalTransaction?  @relation(fields: [transactionId], references: [id], onDelete: SetNull)

  @@index([runId, investorId])
  @@index([status, paidAt])
  @@index([createdById])
  @@index([approvedById])
  @@index([rejectedById])
  @@index([paidById])
  @@index([releasedById])
  @@index([voidedById])
}

model InvestorWithdrawalRequest {
  id                               Int                             @id @default(autoincrement())
  requestNumber                    String                          @unique
  investorId                       Int
  transactionId                    Int?                            @unique
  requestedAmount                  Decimal                         @db.Decimal(14, 2)
  approvedAmount                   Decimal?                        @db.Decimal(14, 2)
  currency                         String                          @default("BDT") @db.VarChar(3)
  availableBalanceSnapshot         Decimal                         @default(0) @db.Decimal(14, 2)
  activeCommittedAmountSnapshot    Decimal                         @default(0) @db.Decimal(14, 2)
  pendingPayoutAmountSnapshot      Decimal                         @default(0) @db.Decimal(14, 2)
  withdrawableBalanceSnapshot      Decimal                         @default(0) @db.Decimal(14, 2)
  beneficiaryNameSnapshot          String?
  beneficiaryBankNameSnapshot      String?
  beneficiaryAccountNumberSnapshot String?
  beneficiaryVerifiedAt            DateTime?
  status                           InvestorWithdrawalRequestStatus @default(REQUESTED)
  requestedSettlementDate          DateTime?
  requestNote                      String?
  reviewNote                       String?
  rejectionReason                  String?
  settlementNote                   String?
  submittedById                    String?
  submittedAt                      DateTime                        @default(now())
  reviewedById                     String?
  reviewedAt                       DateTime?
  settledById                      String?
  settledAt                        DateTime?
  createdAt                        DateTime                        @default(now())
  updatedAt                        DateTime                        @updatedAt
  investor                         Investor                        @relation(fields: [investorId], references: [id], onDelete: Cascade)
  submittedBy                      User?                           @relation("InvestorWithdrawalRequestsSubmittedBy", fields: [submittedById], references: [id], onDelete: SetNull)
  reviewedBy                       User?                           @relation("InvestorWithdrawalRequestsReviewedBy", fields: [reviewedById], references: [id], onDelete: SetNull)
  settledBy                        User?                           @relation("InvestorWithdrawalRequestsSettledBy", fields: [settledById], references: [id], onDelete: SetNull)
  transaction                      InvestorCapitalTransaction?     @relation(fields: [transactionId], references: [id], onDelete: SetNull)

  @@index([investorId, status])
  @@index([status, submittedAt])
  @@index([submittedById])
  @@index([reviewedById])
  @@index([settledById])
}

model Rfq {
  id                          Int                    @id @default(autoincrement())
  rfqNumber                   String                 @unique
  warehouseId                 Int
  purchaseRequisitionId       Int?
  status                      RfqStatus              @default(DRAFT)
  requestedAt                 DateTime               @default(now())
  submissionDeadline          DateTime?
  submittedAt                 DateTime?
  closedAt                    DateTime?
  awardedAt                   DateTime?
  cancelledAt                 DateTime?
  scopeOfWork                 String?
  termsAndConditions          String?
  boqDetails                  String?
  technicalSpecifications     String?
  evaluationCriteria          String?
  resubmissionAllowed         Boolean                @default(true)
  resubmissionRound           Int                    @default(0)
  lastResubmissionRequestedAt DateTime?
  lastResubmissionReason      String?
  createdById                 String?
  approvedById                String?
  currency                    String                 @default("BDT") @db.VarChar(3)
  note                        String?
  sourceRequisitionSnapshot   Json?
  createdAt                   DateTime               @default(now())
  updatedAt                   DateTime               @updatedAt
  approvedBy                  User?                  @relation("RfqsApproved", fields: [approvedById], references: [id], onDelete: SetNull)
  createdBy                   User?                  @relation("RfqsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  purchaseRequisition         PurchaseRequisition?   @relation(fields: [purchaseRequisitionId], references: [id], onDelete: SetNull)
  warehouse                   Warehouse              @relation(fields: [warehouseId], references: [id], onDelete: Restrict)
  items                       RfqItem[]
  attachments                 RfqAttachment[]
  categoryTargets             RfqCategoryTarget[]
  notifications               RfqNotification[]
  supplierInvites             RfqSupplierInvite[]
  quotations                  SupplierQuotation[]
  award                       RfqAward?
  comparativeStatements       ComparativeStatement[]

  @@index([warehouseId, status])
  @@index([purchaseRequisitionId])
  @@index([createdById])
  @@index([approvedById])
  @@index([requestedAt])
  @@index([submissionDeadline])
  @@index([resubmissionRound])
}

model RfqItem {
  id                Int                     @id @default(autoincrement())
  rfqId             Int
  productVariantId  Int
  description       String?
  quantityRequested Int
  targetUnitCost    Decimal?                @db.Decimal(12, 2)
  createdAt         DateTime                @default(now())
  updatedAt         DateTime                @updatedAt
  productVariant    ProductVariant          @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  rfq               Rfq                     @relation(fields: [rfqId], references: [id], onDelete: Cascade)
  quotationItems    SupplierQuotationItem[]

  @@unique([rfqId, productVariantId])
  @@index([rfqId])
  @@index([productVariantId])
}

model RfqSupplierInvite {
  id                      Int                 @id @default(autoincrement())
  rfqId                   Int
  supplierId              Int
  status                  RfqInvitationStatus @default(INVITED)
  invitedAt               DateTime            @default(now())
  respondedAt             DateTime?
  lastNotifiedAt          DateTime?
  resubmissionRequestedAt DateTime?
  resubmissionReason      String?
  note                    String?
  createdById             String?
  createdAt               DateTime            @default(now())
  updatedAt               DateTime            @updatedAt
  createdBy               User?               @relation("RfqSupplierInvitesCreated", fields: [createdById], references: [id], onDelete: SetNull)
  rfq                     Rfq                 @relation(fields: [rfqId], references: [id], onDelete: Cascade)
  supplier                Supplier            @relation(fields: [supplierId], references: [id], onDelete: Restrict)
  quotation               SupplierQuotation?
  notifications           RfqNotification[]

  @@unique([rfqId, supplierId])
  @@index([rfqId, status])
  @@index([supplierId, status])
  @@index([createdById])
}

model SupplierQuotation {
  id                        Int                           @id @default(autoincrement())
  rfqId                     Int
  supplierId                Int
  rfqSupplierInviteId       Int?                          @unique
  status                    QuotationStatus               @default(SUBMITTED)
  revisionNo                Int                           @default(1)
  resubmissionRound         Int                           @default(0)
  resubmissionNote          String?
  quotedAt                  DateTime                      @default(now())
  validUntil                DateTime?
  submittedById             String?
  currency                  String                        @default("BDT") @db.VarChar(3)
  subtotal                  Decimal                       @default(0) @db.Decimal(12, 2)
  taxTotal                  Decimal                       @default(0) @db.Decimal(12, 2)
  total                     Decimal                       @default(0) @db.Decimal(12, 2)
  note                      String?
  technicalProposal         String?
  financialProposal         String?
  createdAt                 DateTime                      @default(now())
  updatedAt                 DateTime                      @updatedAt
  invite                    RfqSupplierInvite?            @relation(fields: [rfqSupplierInviteId], references: [id], onDelete: SetNull)
  rfq                       Rfq                           @relation(fields: [rfqId], references: [id], onDelete: Cascade)
  submittedBy               User?                         @relation("SupplierQuotationsSubmitted", fields: [submittedById], references: [id], onDelete: SetNull)
  supplier                  Supplier                      @relation(fields: [supplierId], references: [id], onDelete: Restrict)
  items                     SupplierQuotationItem[]
  attachments               SupplierQuotationAttachment[]
  comparativeStatementLines ComparativeStatementLine[]
  award                     RfqAward?

  @@unique([rfqId, supplierId])
  @@index([rfqId, status])
  @@index([supplierId, status])
  @@index([submittedById])
  @@index([quotedAt])
}

model SupplierQuotationItem {
  id                  Int               @id @default(autoincrement())
  supplierQuotationId Int
  rfqItemId           Int
  productVariantId    Int
  description         String?
  quantityQuoted      Int
  unitCost            Decimal           @db.Decimal(12, 2)
  lineTotal           Decimal           @db.Decimal(12, 2)
  createdAt           DateTime          @default(now())
  updatedAt           DateTime          @updatedAt
  productVariant      ProductVariant    @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  rfqItem             RfqItem           @relation(fields: [rfqItemId], references: [id], onDelete: Cascade)
  supplierQuotation   SupplierQuotation @relation(fields: [supplierQuotationId], references: [id], onDelete: Cascade)

  @@unique([supplierQuotationId, rfqItemId])
  @@index([supplierQuotationId])
  @@index([rfqItemId])
  @@index([productVariantId])
}

model SupplierQuotationAttachment {
  id                  Int                            @id @default(autoincrement())
  supplierQuotationId Int
  proposalType        SupplierProposalAttachmentType @default(SUPPORTING)
  label               String?
  fileUrl             String
  fileName            String?
  mimeType            String?
  fileSize            Int?
  uploadedById        String?
  createdAt           DateTime                       @default(now())
  supplierQuotation   SupplierQuotation              @relation(fields: [supplierQuotationId], references: [id], onDelete: Cascade)
  uploadedBy          User?                          @relation("SupplierQuotationAttachmentsUploaded", fields: [uploadedById], references: [id], onDelete: SetNull)

  @@index([supplierQuotationId])
  @@index([proposalType])
  @@index([uploadedById])
}

model RfqAward {
  id                  Int               @id @default(autoincrement())
  rfqId               Int               @unique
  supplierId          Int
  supplierQuotationId Int               @unique
  status              RfqAwardStatus    @default(AWARDED)
  awardedAt           DateTime          @default(now())
  awardedById         String?
  purchaseOrderId     Int?              @unique
  note                String?
  createdAt           DateTime          @default(now())
  updatedAt           DateTime          @updatedAt
  awardedBy           User?             @relation("RfqAwardsGranted", fields: [awardedById], references: [id], onDelete: SetNull)
  purchaseOrder       PurchaseOrder?    @relation(fields: [purchaseOrderId], references: [id], onDelete: SetNull)
  rfq                 Rfq               @relation(fields: [rfqId], references: [id], onDelete: Cascade)
  supplier            Supplier          @relation(fields: [supplierId], references: [id], onDelete: Restrict)
  supplierQuotation   SupplierQuotation @relation(fields: [supplierQuotationId], references: [id], onDelete: Restrict)

  @@index([supplierId, status])
  @@index([awardedById])
  @@index([awardedAt])
}

model SupplierCategory {
  id          Int                        @id @default(autoincrement())
  code        String                     @unique
  name        String
  description String?
  isActive    Boolean                    @default(true)
  createdById String?
  createdAt   DateTime                   @default(now())
  updatedAt   DateTime                   @updatedAt
  createdBy   User?                      @relation("SupplierCategoriesCreated", fields: [createdById], references: [id], onDelete: SetNull)
  suppliers   SupplierCategorySupplier[]
  rfqTargets  RfqCategoryTarget[]

  @@index([isActive, name])
  @@index([createdById])
}

model SupplierCategorySupplier {
  id                 Int              @id @default(autoincrement())
  supplierCategoryId Int
  supplierId         Int
  createdById        String?
  createdAt          DateTime         @default(now())
  createdBy          User?            @relation("SupplierCategorySuppliersCreated", fields: [createdById], references: [id], onDelete: SetNull)
  supplierCategory   SupplierCategory @relation(fields: [supplierCategoryId], references: [id], onDelete: Cascade)
  supplier           Supplier         @relation(fields: [supplierId], references: [id], onDelete: Cascade)

  @@unique([supplierCategoryId, supplierId])
  @@index([supplierId])
  @@index([createdById])
}

model RfqCategoryTarget {
  id                 Int              @id @default(autoincrement())
  rfqId              Int
  supplierCategoryId Int
  createdById        String?
  createdAt          DateTime         @default(now())
  createdBy          User?            @relation("RfqCategoryTargetsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  rfq                Rfq              @relation(fields: [rfqId], references: [id], onDelete: Cascade)
  supplierCategory   SupplierCategory @relation(fields: [supplierCategoryId], references: [id], onDelete: Restrict)

  @@unique([rfqId, supplierCategoryId])
  @@index([supplierCategoryId])
  @@index([createdById])
}

model RfqAttachment {
  id           Int      @id @default(autoincrement())
  rfqId        Int
  label        String?
  fileUrl      String
  fileName     String?
  mimeType     String?
  fileSize     Int?
  uploadedById String?
  createdAt    DateTime @default(now())
  rfq          Rfq      @relation(fields: [rfqId], references: [id], onDelete: Cascade)
  uploadedBy   User?    @relation("RfqAttachmentsUploaded", fields: [uploadedById], references: [id], onDelete: SetNull)

  @@index([rfqId])
  @@index([uploadedById])
}

model RfqNotification {
  id             Int                         @id @default(autoincrement())
  rfqId          Int
  inviteId       Int?
  supplierId     Int
  channel        WorkflowNotificationChannel
  status         WorkflowNotificationStatus  @default(PENDING)
  recipientEmail String?
  message        String
  metadata       Json?
  sentAt         DateTime?
  createdById    String?
  createdAt      DateTime                    @default(now())
  createdBy      User?                       @relation("RfqNotificationsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  invite         RfqSupplierInvite?          @relation(fields: [inviteId], references: [id], onDelete: SetNull)
  rfq            Rfq                         @relation(fields: [rfqId], references: [id], onDelete: Cascade)
  supplier       Supplier                    @relation(fields: [supplierId], references: [id], onDelete: Cascade)

  @@index([rfqId, createdAt])
  @@index([inviteId])
  @@index([supplierId, createdAt])
  @@index([status, channel])
  @@index([createdById])
}

model ComparativeStatement {
  id                      Int                                 @id @default(autoincrement())
  csNumber                String                              @unique
  rfqId                   Int
  warehouseId             Int
  versionNo               Int                                 @default(1)
  status                  ComparativeStatementStatus          @default(DRAFT)
  approvalStage           ComparativeStatementApprovalStage   @default(DRAFT)
  technicalWeight         Decimal                             @default(70) @db.Decimal(5, 2)
  financialWeight         Decimal                             @default(30) @db.Decimal(5, 2)
  generatedAt             DateTime                            @default(now())
  submittedAt             DateTime?
  managerApprovedAt       DateTime?
  committeeApprovedAt     DateTime?
  finalApprovedAt         DateTime?
  rejectedAt              DateTime?
  cancelledAt             DateTime?
  managerApprovedById     String?
  committeeApprovedById   String?
  finalApprovedById       String?
  rejectedById            String?
  createdById             String?
  updatedById             String?
  note                    String?
  rejectionNote           String?
  sourceQuotationSnapshot Json?
  createdAt               DateTime                            @default(now())
  updatedAt               DateTime                            @updatedAt
  rfq                     Rfq                                 @relation(fields: [rfqId], references: [id], onDelete: Cascade)
  warehouse               Warehouse                           @relation(fields: [warehouseId], references: [id], onDelete: Restrict)
  createdBy               User?                               @relation("ComparativeStatementsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  updatedBy               User?                               @relation("ComparativeStatementsUpdated", fields: [updatedById], references: [id], onDelete: SetNull)
  managerApprovedBy       User?                               @relation("ComparativeStatementsManagerApproved", fields: [managerApprovedById], references: [id], onDelete: SetNull)
  committeeApprovedBy     User?                               @relation("ComparativeStatementsCommitteeApproved", fields: [committeeApprovedById], references: [id], onDelete: SetNull)
  finalApprovedBy         User?                               @relation("ComparativeStatementsFinalApproved", fields: [finalApprovedById], references: [id], onDelete: SetNull)
  rejectedBy              User?                               @relation("ComparativeStatementsRejected", fields: [rejectedById], references: [id], onDelete: SetNull)
  lines                   ComparativeStatementLine[]
  approvalEvents          ComparativeStatementApprovalEvent[]
  notifications           ComparativeStatementNotification[]
  paymentRequests         PaymentRequest[]
  generatedPurchaseOrder  PurchaseOrder?                      @relation("PurchaseOrderSourceComparativeStatement")

  @@unique([rfqId, versionNo])
  @@index([rfqId, status])
  @@index([warehouseId, status])
  @@index([createdById])
  @@index([managerApprovedById])
  @@index([committeeApprovedById])
  @@index([finalApprovedById])
}

model ComparativeStatementLine {
  id                     Int                  @id @default(autoincrement())
  comparativeStatementId Int
  supplierQuotationId    Int
  supplierId             Int
  financialSubtotal      Decimal              @default(0) @db.Decimal(12, 2)
  financialTaxTotal      Decimal              @default(0) @db.Decimal(12, 2)
  financialGrandTotal    Decimal              @default(0) @db.Decimal(12, 2)
  currency               String               @default("BDT") @db.VarChar(3)
  technicalScore         Decimal              @default(0) @db.Decimal(5, 2)
  financialScore         Decimal              @default(0) @db.Decimal(5, 2)
  combinedScore          Decimal              @default(0) @db.Decimal(7, 4)
  rank                   Int?
  isResponsive           Boolean              @default(true)
  technicalNote          String?
  financialNote          String?
  createdAt              DateTime             @default(now())
  updatedAt              DateTime             @updatedAt
  comparativeStatement   ComparativeStatement @relation(fields: [comparativeStatementId], references: [id], onDelete: Cascade)
  supplierQuotation      SupplierQuotation    @relation(fields: [supplierQuotationId], references: [id], onDelete: Restrict)
  supplier               Supplier             @relation(fields: [supplierId], references: [id], onDelete: Restrict)

  @@unique([comparativeStatementId, supplierQuotationId])
  @@index([comparativeStatementId, rank])
  @@index([supplierId])
}

model ComparativeStatementApprovalEvent {
  id                     Int                                  @id @default(autoincrement())
  comparativeStatementId Int
  stage                  ComparativeStatementApprovalStage
  decision               ComparativeStatementApprovalDecision
  note                   String?
  metadata               Json?
  actedById              String?
  actedAt                DateTime                             @default(now())
  comparativeStatement   ComparativeStatement                 @relation(fields: [comparativeStatementId], references: [id], onDelete: Cascade)
  actedBy                User?                                @relation("ComparativeStatementApprovalEvents", fields: [actedById], references: [id], onDelete: SetNull)

  @@index([comparativeStatementId, stage])
  @@index([actedById])
}

model ComparativeStatementNotification {
  id                     Int                               @id @default(autoincrement())
  comparativeStatementId Int
  stage                  ComparativeStatementApprovalStage
  channel                WorkflowNotificationChannel
  status                 WorkflowNotificationStatus        @default(PENDING)
  recipientUserId        String?
  recipientEmail         String?
  message                String
  metadata               Json?
  sentAt                 DateTime?
  readAt                 DateTime?
  createdAt              DateTime                          @default(now())
  comparativeStatement   ComparativeStatement              @relation(fields: [comparativeStatementId], references: [id], onDelete: Cascade)
  recipientUser          User?                             @relation("ComparativeStatementNotificationsReceived", fields: [recipientUserId], references: [id], onDelete: SetNull)

  @@index([comparativeStatementId, stage, createdAt])
  @@index([recipientUserId])
  @@index([recipientUserId, readAt])
  @@index([status, channel])
}

model PurchaseOrder {
  id                           Int                          @id @default(autoincrement())
  poNumber                     String                       @unique
  supplierId                   Int
  purchaseRequisitionId        Int?
  sourceComparativeStatementId Int?                         @unique
  warehouseId                  Int
  status                       PurchaseOrderStatus          @default(DRAFT)
  approvalStage                PurchaseOrderApprovalStage   @default(DRAFT)
  orderDate                    DateTime                     @default(now())
  expectedAt                   DateTime?
  submittedAt                  DateTime?
  managerApprovedAt            DateTime?
  committeeApprovedAt          DateTime?
  finalApprovedAt              DateTime?
  approvedAt                   DateTime?
  rejectedAt                   DateTime?
  receivedAt                   DateTime?
  createdById                  String?
  approvedById                 String?
  managerApprovedById          String?
  committeeApprovedById        String?
  finalApprovedById            String?
  rejectedById                 String?
  termsTemplateId              Int?
  termsTemplateCode            String?
  termsTemplateName            String?
  termsAndConditions           String?
  rejectionNote                String?
  currency                     String                       @default("BDT") @db.VarChar(3)
  subtotal                     Decimal                      @default(0) @db.Decimal(12, 2)
  taxTotal                     Decimal                      @default(0) @db.Decimal(12, 2)
  shippingTotal                Decimal                      @default(0) @db.Decimal(12, 2)
  grandTotal                   Decimal                      @default(0) @db.Decimal(12, 2)
  notes                        String?
  createdAt                    DateTime                     @default(now())
  updatedAt                    DateTime                     @updatedAt
  approvedBy                   User?                        @relation("PurchaseOrdersApproved", fields: [approvedById], references: [id], onDelete: SetNull)
  managerApprovedBy            User?                        @relation("PurchaseOrdersManagerApproved", fields: [managerApprovedById], references: [id], onDelete: SetNull)
  committeeApprovedBy          User?                        @relation("PurchaseOrdersCommitteeApproved", fields: [committeeApprovedById], references: [id], onDelete: SetNull)
  finalApprovedBy              User?                        @relation("PurchaseOrdersFinalApproved", fields: [finalApprovedById], references: [id], onDelete: SetNull)
  rejectedBy                   User?                        @relation("PurchaseOrdersRejected", fields: [rejectedById], references: [id], onDelete: SetNull)
  createdBy                    User?                        @relation("PurchaseOrdersCreated", fields: [createdById], references: [id], onDelete: SetNull)
  purchaseRequisition          PurchaseRequisition?         @relation(fields: [purchaseRequisitionId], references: [id], onDelete: SetNull)
  sourceComparativeStatement   ComparativeStatement?        @relation("PurchaseOrderSourceComparativeStatement", fields: [sourceComparativeStatementId], references: [id], onDelete: SetNull)
  termsTemplate                PurchaseOrderTermsTemplate?  @relation(fields: [termsTemplateId], references: [id], onDelete: SetNull)
  supplier                     Supplier                     @relation(fields: [supplierId], references: [id], onDelete: Restrict)
  warehouse                    Warehouse                    @relation(fields: [warehouseId], references: [id], onDelete: Restrict)
  goodsReceipts                GoodsReceipt[]
  supplierReturns              SupplierReturn[]
  supplierInvoices             SupplierInvoice[]
  supplierLedgerEntries        SupplierLedgerEntry[]
  rfqAward                     RfqAward?
  items                        PurchaseOrderItem[]
  landedCosts                  PurchaseOrderLandedCost[]
  approvalEvents               PurchaseOrderApprovalEvent[]
  notifications                PurchaseOrderNotification[]
  paymentRequests              PaymentRequest[]

  @@index([supplierId, status])
  @@index([purchaseRequisitionId])
  @@index([sourceComparativeStatementId])
  @@index([warehouseId, status])
  @@index([createdById])
  @@index([approvedById])
  @@index([managerApprovedById])
  @@index([committeeApprovedById])
  @@index([finalApprovedById])
  @@index([rejectedById])
  @@index([termsTemplateId])
  @@index([orderDate])
}

model PurchaseOrderTermsTemplate {
  id             Int             @id @default(autoincrement())
  code           String          @unique
  name           String
  body           String
  isDefault      Boolean         @default(false)
  isActive       Boolean         @default(true)
  createdById    String?
  updatedById    String?
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt
  createdBy      User?           @relation("PurchaseOrderTermsTemplatesCreated", fields: [createdById], references: [id], onDelete: SetNull)
  updatedBy      User?           @relation("PurchaseOrderTermsTemplatesUpdated", fields: [updatedById], references: [id], onDelete: SetNull)
  purchaseOrders PurchaseOrder[]

  @@index([isActive, isDefault])
  @@index([createdById])
  @@index([updatedById])
}

model PurchaseOrderApprovalEvent {
  id              Int                           @id @default(autoincrement())
  purchaseOrderId Int
  stage           PurchaseOrderApprovalStage
  decision        PurchaseOrderApprovalDecision
  note            String?
  metadata        Json?
  actedById       String?
  actedAt         DateTime                      @default(now())
  purchaseOrder   PurchaseOrder                 @relation(fields: [purchaseOrderId], references: [id], onDelete: Cascade)
  actedBy         User?                         @relation("PurchaseOrderApprovalEvents", fields: [actedById], references: [id], onDelete: SetNull)

  @@index([purchaseOrderId, stage])
  @@index([actedById])
}

model PurchaseOrderNotification {
  id              Int                         @id @default(autoincrement())
  purchaseOrderId Int
  stage           PurchaseOrderApprovalStage
  channel         WorkflowNotificationChannel
  status          WorkflowNotificationStatus  @default(PENDING)
  recipientUserId String?
  recipientEmail  String?
  message         String
  metadata        Json?
  sentAt          DateTime?
  readAt          DateTime?
  createdAt       DateTime                    @default(now())
  purchaseOrder   PurchaseOrder               @relation(fields: [purchaseOrderId], references: [id], onDelete: Cascade)
  recipientUser   User?                       @relation("PurchaseOrderNotificationsReceived", fields: [recipientUserId], references: [id], onDelete: SetNull)

  @@index([purchaseOrderId, stage, createdAt])
  @@index([recipientUserId])
  @@index([recipientUserId, readAt])
  @@index([status, channel])
}

model PurchaseOrderItem {
  id                   Int                   @id @default(autoincrement())
  purchaseOrderId      Int
  productVariantId     Int
  description          String?
  quantityOrdered      Int
  quantityReceived     Int                   @default(0)
  unitCost             Decimal               @db.Decimal(12, 2)
  lineTotal            Decimal               @db.Decimal(12, 2)
  createdAt            DateTime              @default(now())
  updatedAt            DateTime              @updatedAt
  purchaseOrder        PurchaseOrder         @relation(fields: [purchaseOrderId], references: [id], onDelete: Cascade)
  productVariant       ProductVariant        @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  receiptItems         GoodsReceiptItem[]
  supplierInvoiceItems SupplierInvoiceItem[]
  supplierReturnItems  SupplierReturnItem[]

  @@index([purchaseOrderId])
  @@index([productVariantId])
}

model PurchaseOrderLandedCost {
  id              Int                              @id @default(autoincrement())
  purchaseOrderId Int
  component       PurchaseOrderLandedCostComponent
  amount          Decimal                          @db.Decimal(12, 2)
  currency        String                           @default("BDT") @db.VarChar(3)
  note            String?
  incurredAt      DateTime                         @default(now())
  createdById     String?
  createdAt       DateTime                         @default(now())
  updatedAt       DateTime                         @updatedAt
  createdBy       User?                            @relation("PurchaseOrderLandedCostsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  purchaseOrder   PurchaseOrder                    @relation(fields: [purchaseOrderId], references: [id], onDelete: Cascade)

  @@index([purchaseOrderId, incurredAt])
  @@index([createdById])
}

model GoodsReceipt {
  id                        Int                            @id @default(autoincrement())
  receiptNumber             String                         @unique
  purchaseOrderId           Int
  warehouseId               Int
  status                    GoodsReceiptStatus             @default(RECEIVED)
  receivedAt                DateTime                       @default(now())
  receivedById              String?
  requesterConfirmedAt      DateTime?
  requesterConfirmedById    String?
  requesterConfirmationNote String?
  note                      String?
  createdAt                 DateTime                       @default(now())
  updatedAt                 DateTime                       @updatedAt
  purchaseOrder             PurchaseOrder                  @relation(fields: [purchaseOrderId], references: [id], onDelete: Restrict)
  warehouse                 Warehouse                      @relation(fields: [warehouseId], references: [id], onDelete: Restrict)
  receivedBy                User?                          @relation("GoodsReceiptsReceived", fields: [receivedById], references: [id], onDelete: SetNull)
  requesterConfirmedBy      User?                          @relation("GoodsReceiptsRequesterConfirmed", fields: [requesterConfirmedById], references: [id], onDelete: SetNull)
  items                     GoodsReceiptItem[]
  attachments               GoodsReceiptAttachment[]
  vendorEvaluations         GoodsReceiptVendorEvaluation[]
  supplierReturns           SupplierReturn[]
  paymentRequests           PaymentRequest[]

  @@index([purchaseOrderId])
  @@index([warehouseId, receivedAt])
  @@index([receivedById])
  @@index([requesterConfirmedById])
  @@index([requesterConfirmedAt])
}

model GoodsReceiptItem {
  id                  Int                  @id @default(autoincrement())
  goodsReceiptId      Int
  purchaseOrderItemId Int
  productVariantId    Int
  quantityReceived    Int
  unitCost            Decimal              @db.Decimal(12, 2)
  createdAt           DateTime             @default(now())
  goodsReceipt        GoodsReceipt         @relation(fields: [goodsReceiptId], references: [id], onDelete: Cascade)
  purchaseOrderItem   PurchaseOrderItem    @relation(fields: [purchaseOrderItemId], references: [id], onDelete: Restrict)
  productVariant      ProductVariant       @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  supplierReturnItems SupplierReturnItem[]

  @@index([goodsReceiptId])
  @@index([purchaseOrderItemId])
  @@index([productVariantId])
}

model GoodsReceiptAttachment {
  id             Int                        @id @default(autoincrement())
  goodsReceiptId Int
  type           GoodsReceiptAttachmentType @default(OTHER)
  fileUrl        String
  fileName       String?
  mimeType       String?
  fileSize       Int?
  note           String?
  uploadedById   String?
  createdAt      DateTime                   @default(now())
  goodsReceipt   GoodsReceipt               @relation(fields: [goodsReceiptId], references: [id], onDelete: Cascade)
  uploadedBy     User?                      @relation("GoodsReceiptAttachmentsUploaded", fields: [uploadedById], references: [id], onDelete: SetNull)

  @@index([goodsReceiptId, createdAt])
  @@index([uploadedById])
  @@index([type, createdAt])
}

model GoodsReceiptVendorEvaluation {
  id                   Int                       @id @default(autoincrement())
  goodsReceiptId       Int
  evaluatorRole        GoodsReceiptEvaluatorRole
  overallRating        Int
  serviceQualityRating Int?
  deliveryRating       Int?
  complianceRating     Int?
  comment              String?
  createdById          String?
  createdAt            DateTime                  @default(now())
  updatedAt            DateTime                  @updatedAt
  createdBy            User?                     @relation("GoodsReceiptVendorEvaluationsBy", fields: [createdById], references: [id], onDelete: SetNull)
  goodsReceipt         GoodsReceipt              @relation(fields: [goodsReceiptId], references: [id], onDelete: Cascade)

  @@unique([goodsReceiptId, evaluatorRole])
  @@index([goodsReceiptId, createdAt])
  @@index([createdById, createdAt])
  @@index([evaluatorRole, createdAt])
}

model SupplierInvoice {
  id                      Int                              @id @default(autoincrement())
  invoiceNumber           String                           @unique
  supplierId              Int
  purchaseOrderId         Int?
  status                  SupplierInvoiceStatus            @default(POSTED)
  matchStatus             ThreeWayMatchStatus              @default(PENDING)
  issueDate               DateTime                         @default(now())
  dueDate                 DateTime?
  postedAt                DateTime                         @default(now())
  createdById             String?
  matchedAt               DateTime?
  matchedById             String?
  currency                String                           @default("BDT") @db.VarChar(3)
  subtotal                Decimal                          @default(0) @db.Decimal(12, 2)
  taxTotal                Decimal                          @default(0) @db.Decimal(12, 2)
  otherCharges            Decimal                          @default(0) @db.Decimal(12, 2)
  total                   Decimal                          @default(0) @db.Decimal(12, 2)
  note                    String?
  paymentHoldStatus       SupplierInvoicePaymentHoldStatus @default(CLEAR)
  paymentHoldReason       String?
  paymentHoldAt           DateTime?
  paymentHoldReleasedAt   DateTime?
  paymentHoldReleasedById String?
  paymentHoldOverrideNote String?
  slaRecommendedCredit    Decimal                          @default(0) @db.Decimal(12, 2)
  slaCreditStatus         SupplierInvoiceSlaCreditStatus   @default(NONE)
  slaCreditReason         String?
  slaCreditUpdatedAt      DateTime?
  createdAt               DateTime                         @default(now())
  updatedAt               DateTime                         @updatedAt
  createdBy               User?                            @relation("SupplierInvoicesCreated", fields: [createdById], references: [id], onDelete: SetNull)
  matchedBy               User?                            @relation("SupplierInvoicesMatched", fields: [matchedById], references: [id], onDelete: SetNull)
  paymentHoldReleasedBy   User?                            @relation("SupplierInvoicePaymentHoldsReleased", fields: [paymentHoldReleasedById], references: [id], onDelete: SetNull)
  purchaseOrder           PurchaseOrder?                   @relation(fields: [purchaseOrderId], references: [id], onDelete: SetNull)
  supplier                Supplier                         @relation(fields: [supplierId], references: [id], onDelete: Restrict)
  items                   SupplierInvoiceItem[]
  payments                SupplierPayment[]
  ledgerEntries           SupplierLedgerEntry[]
  supplierReturns         SupplierReturn[]
  paymentRequests         PaymentRequest[]

  @@index([supplierId, status])
  @@index([purchaseOrderId])
  @@index([createdById])
  @@index([matchedById])
  @@index([matchStatus])
  @@index([paymentHoldStatus])
  @@index([slaCreditStatus])
  @@index([issueDate])
  @@index([dueDate])
  @@index([paymentHoldReleasedById])
}

model SupplierInvoiceItem {
  id                  Int                @id @default(autoincrement())
  supplierInvoiceId   Int
  purchaseOrderItemId Int?
  productVariantId    Int
  description         String?
  quantityInvoiced    Int
  unitCost            Decimal            @db.Decimal(12, 2)
  lineTotal           Decimal            @db.Decimal(12, 2)
  createdAt           DateTime           @default(now())
  updatedAt           DateTime           @updatedAt
  productVariant      ProductVariant     @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  purchaseOrderItem   PurchaseOrderItem? @relation(fields: [purchaseOrderItemId], references: [id], onDelete: SetNull)
  supplierInvoice     SupplierInvoice    @relation(fields: [supplierInvoiceId], references: [id], onDelete: Cascade)

  @@index([supplierInvoiceId])
  @@index([purchaseOrderItemId])
  @@index([productVariantId])
}

model SupplierPayment {
  id                Int                   @id @default(autoincrement())
  paymentNumber     String                @unique
  supplierId        Int
  supplierInvoiceId Int?
  paymentDate       DateTime              @default(now())
  createdById       String?
  amount            Decimal               @db.Decimal(12, 2)
  currency          String                @default("BDT") @db.VarChar(3)
  method            SupplierPaymentMethod
  reference         String?
  note              String?
  createdAt         DateTime              @default(now())
  updatedAt         DateTime              @updatedAt
  createdBy         User?                 @relation("SupplierPaymentsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  supplier          Supplier              @relation(fields: [supplierId], references: [id], onDelete: Restrict)
  supplierInvoice   SupplierInvoice?      @relation(fields: [supplierInvoiceId], references: [id], onDelete: SetNull)
  paymentRequests   PaymentRequest[]
  ledgerEntries     SupplierLedgerEntry[]

  @@index([supplierId, paymentDate])
  @@index([supplierInvoiceId])
  @@index([createdById])
}

model InvestorMasterChangeRequest {
  id               Int                               @id @default(autoincrement())
  investorId       Int
  status           InvestorMasterChangeRequestStatus @default(PENDING)
  requestedChanges Json
  currentSnapshot  Json?
  changeSummary    String?
  reviewNote       String?
  requestedById    String?
  reviewedById     String?
  requestedAt      DateTime                          @default(now())
  reviewedAt       DateTime?
  appliedAt        DateTime?
  createdAt        DateTime                          @default(now())
  updatedAt        DateTime                          @updatedAt
  investor         Investor                          @relation(fields: [investorId], references: [id], onDelete: Cascade)
  requestedBy      User?                             @relation("InvestorMasterChangeRequestsRequestedBy", fields: [requestedById], references: [id], onDelete: SetNull)
  reviewedBy       User?                             @relation("InvestorMasterChangeRequestsReviewedBy", fields: [reviewedById], references: [id], onDelete: SetNull)

  @@index([investorId, status])
  @@index([requestedById])
  @@index([reviewedById])
}

model InvestorProfileUpdateRequest {
  id               Int                                @id @default(autoincrement())
  investorId       Int
  status           InvestorProfileUpdateRequestStatus @default(PENDING)
  requestedChanges Json
  currentSnapshot  Json?
  requestNote      String?
  reviewNote       String?
  submittedById    String?
  reviewedById     String?
  submittedAt      DateTime                           @default(now())
  reviewedAt       DateTime?
  appliedAt        DateTime?
  createdAt        DateTime                           @default(now())
  updatedAt        DateTime                           @updatedAt
  investor         Investor                           @relation(fields: [investorId], references: [id], onDelete: Cascade)
  submittedBy      User?                              @relation("InvestorProfileUpdateRequestsSubmittedBy", fields: [submittedById], references: [id], onDelete: SetNull)
  reviewedBy       User?                              @relation("InvestorProfileUpdateRequestsReviewedBy", fields: [reviewedById], references: [id], onDelete: SetNull)

  @@index([investorId, status])
  @@index([submittedById])
  @@index([reviewedById])
}

model InvestorPortalNotification {
  id          Int                              @id @default(autoincrement())
  investorId  Int
  type        InvestorPortalNotificationType
  title       String
  message     String
  status      InvestorPortalNotificationStatus @default(UNREAD)
  targetUrl   String?
  metadata    Json?
  createdById String?
  readAt      DateTime?
  createdAt   DateTime                         @default(now())
  updatedAt   DateTime                         @updatedAt
  investor    Investor                         @relation(fields: [investorId], references: [id], onDelete: Cascade)
  createdBy   User?                            @relation("InvestorPortalNotificationsCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  @@index([investorId, status])
  @@index([createdById])
  @@index([type, createdAt])
}

model InvestorInternalNotification {
  id          Int                                @id @default(autoincrement())
  userId      String
  type        InvestorInternalNotificationType
  title       String
  message     String
  status      InvestorInternalNotificationStatus @default(UNREAD)
  targetUrl   String?
  entity      String?
  entityId    String?
  metadata    Json?
  createdById String?
  readAt      DateTime?
  createdAt   DateTime                           @default(now())
  updatedAt   DateTime                           @updatedAt
  user        User                               @relation("InvestorInternalNotificationsUser", fields: [userId], references: [id], onDelete: Cascade)
  createdBy   User?                              @relation("InvestorInternalNotificationsCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  @@index([userId, status])
  @@index([createdById])
  @@index([type, createdAt])
  @@index([entity, entityId])
}

model InvestorStatementSchedule {
  id                  Int                                @id @default(autoincrement())
  investorId          Int
  frequency           InvestorStatementScheduleFrequency
  deliveryFormat      InvestorStatementDeliveryFormat    @default(PDF)
  statementWindowDays Int                                @default(30)
  status              InvestorStatementScheduleStatus    @default(ACTIVE)
  nextRunAt           DateTime
  lastRunAt           DateTime?
  lastDispatchedAt    DateTime?
  lastDispatchNote    String?
  createdById         String?
  updatedById         String?
  createdAt           DateTime                           @default(now())
  updatedAt           DateTime                           @updatedAt
  investor            Investor                           @relation(fields: [investorId], references: [id], onDelete: Cascade)
  createdBy           User?                              @relation("InvestorStatementSchedulesCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)
  updatedBy           User?                              @relation("InvestorStatementSchedulesUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  @@index([investorId, status])
  @@index([status, nextRunAt])
  @@index([createdById])
  @@index([updatedById])
}

model InvestorDocument {
  id             Int                                @id @default(autoincrement())
  investorId     Int
  type           InvestorDocumentType
  fileUrl        String
  fileName       String?
  mimeType       String?
  fileSize       Int?
  documentNumber String?
  issuedAt       DateTime?
  expiresAt      DateTime?
  status         InvestorDocumentVerificationStatus @default(PENDING)
  reviewNote     String?
  uploadedById   String?
  reviewedById   String?
  reviewedAt     DateTime?
  createdAt      DateTime                           @default(now())
  updatedAt      DateTime                           @updatedAt
  investor       Investor                           @relation(fields: [investorId], references: [id], onDelete: Cascade)
  uploadedBy     User?                              @relation("InvestorDocumentsUploadedBy", fields: [uploadedById], references: [id], onDelete: SetNull)
  reviewedBy     User?                              @relation("InvestorDocumentsReviewedBy", fields: [reviewedById], references: [id], onDelete: SetNull)

  @@unique([investorId, type])
  @@index([investorId, status])
  @@index([type])
  @@index([uploadedById])
  @@index([reviewedById])
}

model PaymentRequest {
  id                     Int                           @id @default(autoincrement())
  prfNumber              String                        @unique
  supplierId             Int
  warehouseId            Int?
  purchaseOrderId        Int?
  comparativeStatementId Int?
  goodsReceiptId         Int?
  supplierInvoiceId      Int?
  supplierPaymentId      Int?
  status                 PaymentRequestStatus          @default(DRAFT)
  approvalStage          PaymentRequestApprovalStage   @default(DRAFT)
  amount                 Decimal                       @default(0) @db.Decimal(12, 2)
  currency               String                        @default("BDT") @db.VarChar(3)
  requestedAt            DateTime                      @default(now())
  submittedAt            DateTime?
  managerApprovedAt      DateTime?
  financeApprovedAt      DateTime?
  treasuryProcessedAt    DateTime?
  paidAt                 DateTime?
  rejectedAt             DateTime?
  cancelledAt            DateTime?
  createdById            String?
  managerApprovedById    String?
  financeApprovedById    String?
  treasuryProcessedById  String?
  rejectedById           String?
  cancelledById          String?
  note                   String?
  referenceNumber        String?
  createdAt              DateTime                      @default(now())
  updatedAt              DateTime                      @updatedAt
  supplier               Supplier                      @relation(fields: [supplierId], references: [id], onDelete: Restrict)
  warehouse              Warehouse?                    @relation(fields: [warehouseId], references: [id], onDelete: SetNull)
  purchaseOrder          PurchaseOrder?                @relation(fields: [purchaseOrderId], references: [id], onDelete: SetNull)
  comparativeStatement   ComparativeStatement?         @relation(fields: [comparativeStatementId], references: [id], onDelete: SetNull)
  goodsReceipt           GoodsReceipt?                 @relation(fields: [goodsReceiptId], references: [id], onDelete: SetNull)
  supplierInvoice        SupplierInvoice?              @relation(fields: [supplierInvoiceId], references: [id], onDelete: SetNull)
  supplierPayment        SupplierPayment?              @relation(fields: [supplierPaymentId], references: [id], onDelete: SetNull)
  createdBy              User?                         @relation("PaymentRequestsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  managerApprovedBy      User?                         @relation("PaymentRequestsManagerApproved", fields: [managerApprovedById], references: [id], onDelete: SetNull)
  financeApprovedBy      User?                         @relation("PaymentRequestsFinanceApproved", fields: [financeApprovedById], references: [id], onDelete: SetNull)
  treasuryProcessedBy    User?                         @relation("PaymentRequestsTreasuryProcessed", fields: [treasuryProcessedById], references: [id], onDelete: SetNull)
  rejectedBy             User?                         @relation("PaymentRequestsRejected", fields: [rejectedById], references: [id], onDelete: SetNull)
  cancelledBy            User?                         @relation("PaymentRequestsCancelled", fields: [cancelledById], references: [id], onDelete: SetNull)
  approvalEvents         PaymentRequestApprovalEvent[]
  notifications          PaymentRequestNotification[]

  @@index([supplierId, status])
  @@index([warehouseId])
  @@index([purchaseOrderId])
  @@index([comparativeStatementId])
  @@index([goodsReceiptId])
  @@index([supplierInvoiceId])
  @@index([supplierPaymentId])
  @@index([createdById])
  @@index([managerApprovedById])
  @@index([financeApprovedById])
  @@index([treasuryProcessedById])
  @@index([requestedAt])
  @@index([approvalStage])
}

model PaymentRequestApprovalEvent {
  id               Int                            @id @default(autoincrement())
  paymentRequestId Int
  stage            PaymentRequestApprovalStage
  decision         PaymentRequestApprovalDecision
  note             String?
  actedById        String?
  actedAt          DateTime                       @default(now())
  paymentRequest   PaymentRequest                 @relation(fields: [paymentRequestId], references: [id], onDelete: Cascade)
  actedBy          User?                          @relation("PaymentRequestApprovalEvents", fields: [actedById], references: [id], onDelete: SetNull)

  @@index([paymentRequestId, stage])
  @@index([actedById])
}

model PaymentRequestNotification {
  id               Int                         @id @default(autoincrement())
  paymentRequestId Int
  stage            PaymentRequestApprovalStage
  channel          WorkflowNotificationChannel
  status           WorkflowNotificationStatus  @default(PENDING)
  recipientUserId  String?
  recipientEmail   String?
  message          String
  metadata         Json?
  sentAt           DateTime?
  readAt           DateTime?
  createdAt        DateTime                    @default(now())
  createdById      String?
  paymentRequest   PaymentRequest              @relation(fields: [paymentRequestId], references: [id], onDelete: Cascade)
  recipientUser    User?                       @relation("PaymentRequestNotificationsReceived", fields: [recipientUserId], references: [id], onDelete: SetNull)
  createdBy        User?                       @relation("PaymentRequestNotificationsCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  @@index([paymentRequestId, stage, createdAt])
  @@index([recipientUserId])
  @@index([recipientUserId, readAt])
  @@index([status, channel])
  @@index([createdById])
}

model SupplierLedgerEntry {
  id                Int                     @id @default(autoincrement())
  supplierId        Int
  entryDate         DateTime                @default(now())
  entryType         SupplierLedgerEntryType
  direction         SupplierLedgerDirection
  amount            Decimal                 @db.Decimal(12, 2)
  currency          String                  @default("BDT") @db.VarChar(3)
  note              String?
  referenceType     String?
  referenceNumber   String?
  purchaseOrderId   Int?
  supplierInvoiceId Int?
  supplierPaymentId Int?
  supplierReturnId  Int?
  createdById       String?
  createdAt         DateTime                @default(now())
  updatedAt         DateTime                @updatedAt
  createdBy         User?                   @relation("SupplierLedgerEntriesCreated", fields: [createdById], references: [id], onDelete: SetNull)
  purchaseOrder     PurchaseOrder?          @relation(fields: [purchaseOrderId], references: [id], onDelete: SetNull)
  supplier          Supplier                @relation(fields: [supplierId], references: [id], onDelete: Restrict)
  supplierInvoice   SupplierInvoice?        @relation(fields: [supplierInvoiceId], references: [id], onDelete: SetNull)
  supplierPayment   SupplierPayment?        @relation(fields: [supplierPaymentId], references: [id], onDelete: SetNull)
  supplierReturn    SupplierReturn?         @relation(fields: [supplierReturnId], references: [id], onDelete: SetNull)

  @@index([supplierId, entryDate])
  @@index([supplierInvoiceId])
  @@index([supplierPaymentId])
  @@index([supplierReturnId])
  @@index([purchaseOrderId])
  @@index([createdById])
}

model SupplierReturn {
  id                Int                   @id @default(autoincrement())
  returnNumber      String                @unique
  supplierId        Int
  warehouseId       Int
  purchaseOrderId   Int?
  goodsReceiptId    Int
  supplierInvoiceId Int?
  status            SupplierReturnStatus  @default(DRAFT)
  requestedAt       DateTime              @default(now())
  requiredBy        DateTime?
  submittedAt       DateTime?
  approvedAt        DateTime?
  dispatchedAt      DateTime?
  closedAt          DateTime?
  ledgerPostedAt    DateTime?
  createdById       String?
  approvedById      String?
  dispatchedById    String?
  closedById        String?
  reasonCode        String?
  note              String?
  createdAt         DateTime              @default(now())
  updatedAt         DateTime              @updatedAt
  approvedBy        User?                 @relation("SupplierReturnsApproved", fields: [approvedById], references: [id], onDelete: SetNull)
  closedBy          User?                 @relation("SupplierReturnsClosed", fields: [closedById], references: [id], onDelete: SetNull)
  createdBy         User?                 @relation("SupplierReturnsCreated", fields: [createdById], references: [id], onDelete: SetNull)
  dispatchedBy      User?                 @relation("SupplierReturnsDispatched", fields: [dispatchedById], references: [id], onDelete: SetNull)
  goodsReceipt      GoodsReceipt          @relation(fields: [goodsReceiptId], references: [id], onDelete: Restrict)
  items             SupplierReturnItem[]
  ledgerEntries     SupplierLedgerEntry[]
  purchaseOrder     PurchaseOrder?        @relation(fields: [purchaseOrderId], references: [id], onDelete: SetNull)
  supplier          Supplier              @relation(fields: [supplierId], references: [id], onDelete: Restrict)
  supplierInvoice   SupplierInvoice?      @relation(fields: [supplierInvoiceId], references: [id], onDelete: SetNull)
  warehouse         Warehouse             @relation(fields: [warehouseId], references: [id], onDelete: Restrict)

  @@index([supplierId, status])
  @@index([warehouseId, status])
  @@index([goodsReceiptId])
  @@index([purchaseOrderId])
  @@index([supplierInvoiceId])
  @@index([createdById])
  @@index([approvedById])
  @@index([requestedAt])
}

model SupplierReturnItem {
  id                  Int                @id @default(autoincrement())
  supplierReturnId    Int
  goodsReceiptItemId  Int?
  purchaseOrderItemId Int?
  productVariantId    Int
  description         String?
  quantityRequested   Int
  quantityDispatched  Int                @default(0)
  unitCost            Decimal            @db.Decimal(12, 2)
  lineTotal           Decimal            @db.Decimal(12, 2)
  reason              String?
  createdAt           DateTime           @default(now())
  updatedAt           DateTime           @updatedAt
  goodsReceiptItem    GoodsReceiptItem?  @relation(fields: [goodsReceiptItemId], references: [id], onDelete: SetNull)
  productVariant      ProductVariant     @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  purchaseOrderItem   PurchaseOrderItem? @relation(fields: [purchaseOrderItemId], references: [id], onDelete: SetNull)
  supplierReturn      SupplierReturn     @relation(fields: [supplierReturnId], references: [id], onDelete: Cascade)

  @@index([supplierReturnId])
  @@index([goodsReceiptItemId])
  @@index([purchaseOrderItemId])
  @@index([productVariantId])
}

model ReplenishmentRule {
  id               Int                   @id @default(autoincrement())
  warehouseId      Int
  productVariantId Int
  strategy         ReplenishmentStrategy @default(MIN_MAX)
  reorderPoint     Int
  targetStockLevel Int
  safetyStock      Int                   @default(0)
  minOrderQty      Int                   @default(1)
  orderMultiple    Int                   @default(1)
  leadTimeDays     Int?
  isActive         Boolean               @default(true)
  note             String?
  createdById      String?
  updatedById      String?
  createdAt        DateTime              @default(now())
  updatedAt        DateTime              @updatedAt
  createdBy        User?                 @relation("ReplenishmentRulesCreated", fields: [createdById], references: [id], onDelete: SetNull)
  productVariant   ProductVariant        @relation(fields: [productVariantId], references: [id], onDelete: Cascade)
  updatedBy        User?                 @relation("ReplenishmentRulesUpdated", fields: [updatedById], references: [id], onDelete: SetNull)
  warehouse        Warehouse             @relation(fields: [warehouseId], references: [id], onDelete: Cascade)

  @@unique([warehouseId, productVariantId])
  @@index([warehouseId, isActive])
  @@index([productVariantId, isActive])
  @@index([createdById])
  @@index([updatedById])
}

model SupplierSlaPolicy {
  id                           Int                          @id @default(autoincrement())
  supplierId                   Int                          @unique
  isActive                     Boolean                      @default(true)
  effectiveFrom                DateTime                     @default(now())
  effectiveTo                  DateTime?
  evaluationWindowDays         Int                          @default(90)
  minTrackedPoCount            Int                          @default(3)
  targetLeadTimeDays           Int
  minimumOnTimeRate            Decimal                      @default(90) @db.Decimal(5, 2)
  minimumFillRate              Decimal                      @default(95) @db.Decimal(5, 2)
  maxOpenLatePoCount           Int                          @default(0)
  autoEvaluationEnabled        Boolean                      @default(true)
  warningActionDueDays         Int                          @default(7)
  breachActionDueDays          Int                          @default(3)
  terminationClauseEnabled     Boolean                      @default(false)
  terminationLookbackDays      Int                          @default(180)
  terminationMinBreachCount    Int                          @default(3)
  terminationMinCriticalCount  Int                          @default(1)
  terminationRecommendedAction SupplierSlaTerminationAction @default(REVIEW_CONTRACT)
  terminationNote              String?
  note                         String?
  createdById                  String?
  updatedById                  String?
  createdAt                    DateTime                     @default(now())
  updatedAt                    DateTime                     @updatedAt
  breaches                     SupplierSlaBreach[]
  financialRule                SupplierSlaFinancialRule?
  terminationCases             SupplierSlaTerminationCase[]
  createdBy                    User?                        @relation("SupplierSlaPoliciesCreated", fields: [createdById], references: [id], onDelete: SetNull)
  supplier                     Supplier                     @relation(fields: [supplierId], references: [id], onDelete: Cascade)
  updatedBy                    User?                        @relation("SupplierSlaPoliciesUpdated", fields: [updatedById], references: [id], onDelete: SetNull)

  @@index([isActive])
  @@index([createdById])
  @@index([updatedById])
}

model SupplierSlaFinancialRule {
  id                              Int               @id @default(autoincrement())
  supplierSlaPolicyId             Int               @unique
  isActive                        Boolean           @default(true)
  holdPaymentsOnThreeWayVariance  Boolean           @default(true)
  holdPaymentsOnOpenSlaAction     Boolean           @default(true)
  allowPaymentHoldOverride        Boolean           @default(true)
  autoCreditRecommendationEnabled Boolean           @default(true)
  autoApplyRecommendedCredit      Boolean           @default(false)
  autoApplyRequireMatchedInvoice  Boolean           @default(true)
  autoApplyBlockOnOpenDispute     Boolean           @default(true)
  warningPenaltyRatePercent       Decimal           @default(0) @db.Decimal(5, 2)
  breachPenaltyRatePercent        Decimal           @default(2) @db.Decimal(5, 2)
  criticalPenaltyRatePercent      Decimal           @default(5) @db.Decimal(5, 2)
  minBreachCountForCredit         Int               @default(1)
  autoApplyMaxAmount              Decimal?          @db.Decimal(12, 2)
  maxCreditCapAmount              Decimal?          @db.Decimal(12, 2)
  note                            String?
  createdById                     String?
  updatedById                     String?
  createdAt                       DateTime          @default(now())
  updatedAt                       DateTime          @updatedAt
  createdBy                       User?             @relation("SupplierSlaFinancialRulesCreated", fields: [createdById], references: [id], onDelete: SetNull)
  policy                          SupplierSlaPolicy @relation(fields: [supplierSlaPolicyId], references: [id], onDelete: Cascade)
  updatedBy                       User?             @relation("SupplierSlaFinancialRulesUpdated", fields: [updatedById], references: [id], onDelete: SetNull)

  @@index([isActive])
  @@index([createdById])
  @@index([updatedById])
}

model SupplierSlaBreach {
  id                        Int                          @id @default(autoincrement())
  supplierSlaPolicyId       Int
  supplierId                Int
  evaluationDate            DateTime                     @default(now())
  periodStart               DateTime
  periodEnd                 DateTime
  trackedPoCount            Int                          @default(0)
  completedPoCount          Int                          @default(0)
  openLatePoCount           Int                          @default(0)
  breachCount               Int                          @default(0)
  status                    SupplierSlaEvaluationStatus  @default(OK)
  severity                  SupplierSlaSeverity          @default(LOW)
  observedLeadTimeDays      Decimal?                     @db.Decimal(8, 2)
  onTimeRatePercent         Decimal?                     @db.Decimal(5, 2)
  fillRatePercent           Decimal?                     @db.Decimal(5, 2)
  actionStatus              SupplierSlaActionStatus      @default(NOT_REQUIRED)
  ownerUserId               String?
  dueDate                   DateTime?
  startedAt                 DateTime?
  resolvedAt                DateTime?
  resolvedById              String?
  resolutionNote            String?
  alertTriggeredAt          DateTime?
  alertMessage              String?
  alertAcknowledgedAt       DateTime?
  disputeStatus             SupplierSlaDisputeStatus     @default(NONE)
  disputeReason             String?
  disputeRaisedAt           DateTime?
  disputeRaisedById         String?
  disputeResolutionNote     String?
  disputeResolvedAt         DateTime?
  disputeResolvedById       String?
  terminationCaseId         Int?
  terminationSuggestedAt    DateTime?
  terminationSuggestionNote String?
  issues                    Json?
  evaluatedById             String?
  createdAt                 DateTime                     @default(now())
  updatedAt                 DateTime                     @updatedAt
  disputeRaisedBy           User?                        @relation("SupplierSlaBreachesDisputeRaised", fields: [disputeRaisedById], references: [id], onDelete: SetNull)
  disputeResolvedBy         User?                        @relation("SupplierSlaBreachesDisputeResolved", fields: [disputeResolvedById], references: [id], onDelete: SetNull)
  owner                     User?                        @relation("SupplierSlaBreachesOwned", fields: [ownerUserId], references: [id], onDelete: SetNull)
  resolvedBy                User?                        @relation("SupplierSlaBreachesResolved", fields: [resolvedById], references: [id], onDelete: SetNull)
  evaluatedBy               User?                        @relation("SupplierSlaBreachesEvaluated", fields: [evaluatedById], references: [id], onDelete: SetNull)
  policy                    SupplierSlaPolicy            @relation(fields: [supplierSlaPolicyId], references: [id], onDelete: Cascade)
  supplier                  Supplier                     @relation(fields: [supplierId], references: [id], onDelete: Cascade)
  terminationCase           SupplierSlaTerminationCase?  @relation(fields: [terminationCaseId], references: [id], onDelete: SetNull)
  triggeredTerminationCases SupplierSlaTerminationCase[] @relation("SupplierSlaTerminationTriggerBreach")

  @@index([supplierId, evaluationDate])
  @@index([supplierSlaPolicyId, evaluationDate])
  @@index([status, evaluationDate])
  @@index([actionStatus, dueDate])
  @@index([disputeStatus, evaluationDate])
  @@index([ownerUserId, actionStatus])
  @@index([disputeRaisedById])
  @@index([disputeResolvedById])
  @@index([terminationCaseId])
  @@index([resolvedById])
  @@index([evaluatedById])
}

model SupplierSlaTerminationCase {
  id                  Int                              @id @default(autoincrement())
  supplierId          Int
  supplierSlaPolicyId Int
  triggerBreachId     Int?
  status              SupplierSlaTerminationCaseStatus @default(OPEN)
  recommendedAction   SupplierSlaTerminationAction     @default(REVIEW_CONTRACT)
  openBreachCount     Int                              @default(0)
  criticalBreachCount Int                              @default(0)
  lookbackDays        Int                              @default(180)
  reason              String
  ownerUserId         String?
  reviewedAt          DateTime?
  resolvedAt          DateTime?
  resolvedById        String?
  resolutionNote      String?
  createdById         String?
  createdAt           DateTime                         @default(now())
  updatedAt           DateTime                         @updatedAt
  createdBy           User?                            @relation("SupplierSlaTerminationCasesCreated", fields: [createdById], references: [id], onDelete: SetNull)
  owner               User?                            @relation("SupplierSlaTerminationCasesOwned", fields: [ownerUserId], references: [id], onDelete: SetNull)
  policy              SupplierSlaPolicy                @relation(fields: [supplierSlaPolicyId], references: [id], onDelete: Cascade)
  resolvedBy          User?                            @relation("SupplierSlaTerminationCasesResolved", fields: [resolvedById], references: [id], onDelete: SetNull)
  supplier            Supplier                         @relation(fields: [supplierId], references: [id], onDelete: Cascade)
  triggerBreach       SupplierSlaBreach?               @relation("SupplierSlaTerminationTriggerBreach", fields: [triggerBreachId], references: [id], onDelete: SetNull)
  breaches            SupplierSlaBreach[]

  @@index([supplierId, status])
  @@index([supplierSlaPolicyId, status])
  @@index([ownerUserId, status])
  @@index([resolvedById])
  @@index([createdById])
  @@index([createdAt])
}

model WarehouseTransfer {
  id                     Int                     @id @default(autoincrement())
  transferNumber         String                  @unique
  sourceWarehouseId      Int
  destinationWarehouseId Int
  status                 WarehouseTransferStatus @default(DRAFT)
  requestedAt            DateTime                @default(now())
  requiredBy             DateTime?
  submittedAt            DateTime?
  approvedAt             DateTime?
  dispatchedAt           DateTime?
  receivedAt             DateTime?
  createdById            String?
  approvedById           String?
  dispatchedById         String?
  receivedById           String?
  note                   String?
  createdAt              DateTime                @default(now())
  updatedAt              DateTime                @updatedAt
  approvedBy             User?                   @relation("WarehouseTransfersApproved", fields: [approvedById], references: [id], onDelete: SetNull)
  createdBy              User?                   @relation("WarehouseTransfersCreated", fields: [createdById], references: [id], onDelete: SetNull)
  destinationWarehouse   Warehouse               @relation("WarehouseTransferDestination", fields: [destinationWarehouseId], references: [id], onDelete: Restrict)
  dispatchedBy           User?                   @relation("WarehouseTransfersDispatched", fields: [dispatchedById], references: [id], onDelete: SetNull)
  items                  WarehouseTransferItem[]
  receivedBy             User?                   @relation("WarehouseTransfersReceived", fields: [receivedById], references: [id], onDelete: SetNull)
  sourceWarehouse        Warehouse               @relation("WarehouseTransferSource", fields: [sourceWarehouseId], references: [id], onDelete: Restrict)

  @@index([sourceWarehouseId, status])
  @@index([destinationWarehouseId, status])
  @@index([createdById])
  @@index([approvedById])
  @@index([requestedAt])
}

model WarehouseTransferItem {
  id                  Int               @id @default(autoincrement())
  warehouseTransferId Int
  productVariantId    Int
  description         String?
  quantityRequested   Int
  quantityDispatched  Int               @default(0)
  quantityReceived    Int               @default(0)
  createdAt           DateTime          @default(now())
  updatedAt           DateTime          @updatedAt
  productVariant      ProductVariant    @relation(fields: [productVariantId], references: [id], onDelete: Restrict)
  warehouseTransfer   WarehouseTransfer @relation(fields: [warehouseTransferId], references: [id], onDelete: Cascade)

  @@index([warehouseTransferId])
  @@index([productVariantId])
}

model NewsletterSubscriber {
  id             String    @id @default(uuid())
  email          String    @unique
  status         String    @default("subscribed")
  unsubscribedAt DateTime?
  createdAt      DateTime  @default(now())
}

model Newsletter {
  id        String    @id @default(uuid())
  title     String
  subject   String
  content   String
  status    String    @default("draft")
  sentAt    DateTime?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model Coupon {
  id            String    @id @default(uuid())
  code          String    @unique
  discountType  String
  discountValue Decimal   @db.Decimal(10, 2)
  minOrderValue Decimal?  @db.Decimal(10, 2)
  maxDiscount   Decimal?  @db.Decimal(10, 2)
  usageLimit    Int?
  usedCount     Int       @default(0)
  isValid       Boolean   @default(true)
  expiresAt     DateTime?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  orders        Order[]
}

model PasswordResetToken {
  id        String   @id @default(uuid())
  email     String   @db.VarChar(255)
  token     String   @unique @db.VarChar(255)
  expiresAt DateTime
  createdAt DateTime @default(now())

  @@index([email])
  @@index([token])
}

model sitesettings {
  id                Int     @id @default(autoincrement())
  logo              String?
  siteTitle         String?
  footerDescription String?
  contactNumber     String?
  contactEmail      String?
  address           String?
  facebookLink      String?
  instagramLink     String?
  twitterLink       String?
  tiktokLink        String?
  youtubeLink       String?
}

model Banner {
  id          Int        @id @default(autoincrement())
  title       String
  subtitle    String?
  description String?
  image       String
  mobileImage String?
  buttonText  String?
  buttonLink  String?
  position    Int        @default(0)
  isActive    Boolean    @default(true)
  startDate   DateTime?
  endDate     DateTime?
  type        BannerType @default(HERO)
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
}

model DeliveryManProfile {
  id           String  @id @default(cuid())
  userId       String  @unique
  warehouseId  Int
  employeeCode String? @unique

  // Personal info
  fullName       String
  phone          String
  alternatePhone String?
  email          String?
  dateOfBirth    DateTime?
  gender         String?

  presentAddress   String
  permanentAddress String

  emergencyContactName     String?
  emergencyContactPhone    String?
  emergencyContactRelation String?

  // Identity info (self)
  identityType       IdentityType
  identityNumber     String
  passportExpiryDate DateTime?

  // Parents info
  fatherName           String
  fatherIdentityType   IdentityType?
  fatherIdentityNumber String?

  motherName           String
  motherIdentityType   IdentityType?
  motherIdentityNumber String?

  // Bank / legal
  bankName          String?
  bankAccountName   String?
  bankAccountNumber String?
  bankChequeNumber  String?

  bondAmount     Decimal?  @db.Decimal(10, 2)
  bondSignedAt   DateTime?
  bondExpiryDate DateTime?

  contractSignedAt  DateTime?
  contractStartDate DateTime?
  contractEndDate   DateTime?
  contractStatus    String?

  joiningDate       DateTime
  status            DeliveryManStatus         @default(PENDING)
  applicationStatus DeliveryApplicationStatus @default(DRAFT)

  verifiedById    String?
  verifiedAt      DateTime?
  rejectionReason String?
  note            String?

  assignedById String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relations
  user       User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  warehouse  Warehouse @relation(fields: [warehouseId], references: [id], onDelete: Cascade)
  verifiedBy User?     @relation("VerifiedDeliveryMen", fields: [verifiedById], references: [id], onDelete: SetNull)
  assignedBy User?     @relation("AssignedDeliveryMen", fields: [assignedById], references: [id], onDelete: SetNull)

  references          DeliveryManReference[]
  documents           DeliveryManDocument[]
  deliveryAssignments DeliveryAssignment[]

  @@index([warehouseId])
  @@index([status])
  @@index([applicationStatus])
  @@index([verifiedById])
  @@index([assignedById])
}

model DeliveryManReference {
  id                   String @id @default(cuid())
  deliveryManProfileId String

  name       String
  phone      String
  relation   String?
  address    String?
  occupation String?

  identityType   IdentityType @default(NID)
  identityNumber String

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  profile   DeliveryManProfile    @relation(fields: [deliveryManProfileId], references: [id], onDelete: Cascade)
  documents DeliveryManDocument[]

  @@index([deliveryManProfileId])
}

model DeliveryManDocument {
  id                   String  @id @default(cuid())
  deliveryManProfileId String
  referenceId          String?

  type     DeliveryManDocumentType
  title    String?
  fileUrl  String
  fileName String?
  mimeType String?
  fileSize Int?
  note     String?

  isVerified   Boolean   @default(false)
  verifiedAt   DateTime?
  verifiedById String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  profile    DeliveryManProfile    @relation(fields: [deliveryManProfileId], references: [id], onDelete: Cascade)
  reference  DeliveryManReference? @relation(fields: [referenceId], references: [id], onDelete: Cascade)
  verifiedBy User?                 @relation("VerifiedDeliveryManDocuments", fields: [verifiedById], references: [id], onDelete: SetNull)

  @@index([deliveryManProfileId])
  @@index([referenceId])
  @@index([type])
  @@index([verifiedById])
}

model DeliveryAssignment {
  id                   String                   @id @default(cuid())
  orderId              Int
  shipmentId           Int
  deliveryManProfileId String
  warehouseId          Int
  assignedById         String?
  status               DeliveryAssignmentStatus @default(ASSIGNED)
  pickupProofStatus    PickupProofStatus        @default(PENDING)
  isCurrent            Boolean                  @default(true)
  rejectionReason      String?
  note                 String?
  latestNote           String?
  assignedAt           DateTime                 @default(now())
  respondedAt          DateTime?
  acceptedAt           DateTime?
  rejectedAt           DateTime?
  pickupConfirmedAt    DateTime?
  inTransitAt          DateTime?
  outForDeliveryAt     DateTime?
  deliveredAt          DateTime?
  deliveredLatitude    Float?
  deliveredLongitude   Float?
  deliveredAccuracy    Float?
  failedAt             DateTime?
  returnedAt           DateTime?
  completedAt          DateTime?
  createdAt            DateTime                 @default(now())
  updatedAt            DateTime                 @updatedAt
  assignedBy           User?                    @relation("CreatedDeliveryAssignments", fields: [assignedById], references: [id], onDelete: SetNull)
  deliveryMan          DeliveryManProfile       @relation(fields: [deliveryManProfileId], references: [id], onDelete: Cascade)
  order                Order                    @relation(fields: [orderId], references: [id], onDelete: Cascade)
  pickupProof          WarehousePickupProof?
  shipment             Shipment                 @relation(fields: [shipmentId], references: [id], onDelete: Cascade)
  warehouse            Warehouse                @relation(fields: [warehouseId], references: [id], onDelete: Cascade)
  logs                 DeliveryAssignmentLog[]

  @@index([orderId])
  @@index([shipmentId])
  @@index([deliveryManProfileId, status])
  @@index([deliveryManProfileId, isCurrent])
  @@index([warehouseId, status])
  @@index([assignedById])
  @@index([shipmentId, isCurrent])
}

model DeliveryAssignmentLog {
  id                   String                    @id @default(cuid())
  deliveryAssignmentId String
  fromStatus           DeliveryAssignmentStatus?
  toStatus             DeliveryAssignmentStatus
  note                 String?
  actorUserId          String?
  createdAt            DateTime                  @default(now())
  actor                User?                     @relation("DeliveryAssignmentLogsCreated", fields: [actorUserId], references: [id], onDelete: SetNull)
  deliveryAssignment   DeliveryAssignment        @relation(fields: [deliveryAssignmentId], references: [id], onDelete: Cascade)

  @@index([deliveryAssignmentId, createdAt])
  @@index([actorUserId])
}

model WarehousePickupProof {
  id                     String             @id @default(cuid())
  deliveryAssignmentId   String             @unique
  actorUserId            String?
  status                 PickupProofStatus  @default(PENDING)
  productReceived        Boolean
  packagingOk            Boolean
  productInGoodCondition Boolean
  imageUrl               String?
  note                   String?
  confirmedAt            DateTime?
  createdAt              DateTime           @default(now())
  updatedAt              DateTime           @updatedAt
  actor                  User?              @relation("WarehousePickupProofsCreated", fields: [actorUserId], references: [id], onDelete: SetNull)
  deliveryAssignment     DeliveryAssignment @relation(fields: [deliveryAssignmentId], references: [id], onDelete: Cascade)

  @@index([actorUserId])
  @@index([confirmedAt])
}

model AnalyticsEvent {
  id            BigInt             @id @default(autoincrement())
  ts            DateTime           @default(now())
  event         AnalyticsEventName
  visitorId     String             @map("visitor_id")
  sessionId     String             @map("session_id")
  userId        String?            @map("user_id")
  path          String             @default("/") @map("path")
  title         String?            @map("title")
  referrer      String?            @map("referrer")
  utmSource     String?            @map("utm_source")
  utmMedium     String?            @map("utm_medium")
  utmCampaign   String?            @map("utm_campaign")
  deviceType    String?            @map("device_type")
  browser       String?            @map("browser")
  os            String?            @map("os")
  screen        String?            @map("screen")
  lang          String?            @map("lang")
  country       String?            @map("country")
  city          String?            @map("city")
  activeSeconds Int                @default(0) @map("active_seconds")
  ipHash        String?            @map("ip_hash")
  dayKey        String             @default("") @map("day_key")
  user          User?              @relation(fields: [userId], references: [id])

  @@index([dayKey])
  @@index([ipHash, dayKey])
  @@index([ts])
  @@index([event, ts])
  @@index([visitorId, ts])
  @@index([sessionId, ts])
  @@index([path, ts])
  @@index([utmSource, ts])
  @@index([deviceType, ts])
  @@map("analytics_events")
}

model GeoIpCache {
  id        Int      @id @default(autoincrement())
  ip        String   @unique
  country   String?
  city      String?
  region    String?
  lat       Float?
  lon       Float?
  isp       String?
  updatedAt DateTime @updatedAt
  createdAt DateTime @default(now())

  @@index([updatedAt])
  @@map("geo_ip_cache")
}

model ChatConversation {
  id            String        @id @default(cuid())
  userId        String?
  guestEmail    String?
  guestName     String?
  status        ChatStatus    @default(OPEN)
  priority      ChatPriority  @default(NORMAL)
  assignedToId  String?
  lastMessageAt DateTime?
  closedAt      DateTime?
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
  assignedTo    User?         @relation("AssignedChats", fields: [assignedToId], references: [id])
  user          User?         @relation("UserChats", fields: [userId], references: [id])
  messages      ChatMessage[]

  @@index([status])
  @@index([assignedToId])
}

model ChatMessage {
  id             String           @id @default(cuid())
  conversationId String
  senderId       String?
  senderRole     String
  message        String
  attachmentUrl  String?
  isRead         Boolean          @default(false)
  createdAt      DateTime         @default(now())
  conversation   ChatConversation @relation(fields: [conversationId], references: [id])
  sender         User?            @relation(fields: [senderId], references: [id])

  @@index([conversationId])
}

model ActivityLog {
  id        BigInt   @id @default(autoincrement())
  userId    String?
  action    String
  entity    String
  entityId  String?
  metadata  Json?
  ipHash    String?
  userAgent String?
  createdAt DateTime @default(now())
  updatetAt DateTime
  user      User?    @relation(fields: [userId], references: [id])

  @@index([entity, entityId])
  @@index([userId])
  @@index([createdAt])
}

enum ProductType {
  PHYSICAL
  DIGITAL
  SERVICE
  BUNDLE
}

enum InventoryItemClass {
  CONSUMABLE
  PERMANENT
}

enum ProductCodeKind {
  BARCODE
  QRCODE
}

enum ProductCodeSymbology {
  CODE128
  EAN13
  QR
}

enum MaterialRequestStatus {
  DRAFT
  SUBMITTED
  SUPERVISOR_ENDORSED
  PROJECT_MANAGER_ENDORSED
  ADMIN_APPROVED
  PARTIALLY_RELEASED
  RELEASED
  REJECTED
  CANCELLED
}

enum MaterialRequestApprovalStage {
  SUBMISSION
  SUPERVISOR_ENDORSEMENT
  PROJECT_MANAGER_ENDORSEMENT
  ADMIN_APPROVAL
  REJECTION
  CANCELLATION
}

enum MaterialRequestApprovalDecision {
  APPROVED
  REJECTED
  CANCELLED
}

enum MaterialReleaseStatus {
  ISSUED
  CANCELLED
}

enum AssetRegisterStatus {
  ACTIVE
  RETIRED
  LOST
  DISPOSED
}

enum InventoryVerificationFrequency {
  MONTHLY
  QUARTERLY
  ANNUAL
}

enum InventoryVerificationStatus {
  DRAFT
  SUBMITTED
  COMMITTEE_REVIEW
  APPROVED
  REJECTED
  CLOSED
}

enum InventoryVerificationApprovalStage {
  SUBMISSION
  COMMITTEE
  ADMIN
}

enum InventoryVerificationApprovalDecision {
  APPROVED
  REJECTED
}

enum ReorderAlertStatus {
  OPEN
  ACKNOWLEDGED
  RESOLVED
}

enum PaymentRequestStatus {
  DRAFT
  SUBMITTED
  MANAGER_APPROVED
  FINANCE_APPROVED
  TREASURY_PROCESSING
  PAID
  REJECTED
  CANCELLED
}

enum PaymentRequestApprovalStage {
  DRAFT
  SUBMISSION
  MANAGER_REVIEW
  FINANCE_REVIEW
  TREASURY
  PAID
  REJECTION
  CANCELLATION
}

enum PaymentRequestApprovalDecision {
  SUBMITTED
  APPROVED
  REJECTED
  CANCELLED
  PAID
}

enum OrderStatus {
  PENDING
  CONFIRMED
  PROCESSING
  SHIPPED
  DELIVERED
  CANCELLED
  RETURNED
  FAILED
}

enum PaymentStatus {
  UNPAID
  PAID
  REFUNDED
}

enum PaymentState {
  INITIATED
  AUTHORIZED
  CAPTURED
  FAILED
  REFUNDED
  VOIDED
}

enum BookingStatus {
  PENDING
  CONFIRMED
  CANCELLED
  COMPLETED
}

enum ShipmentStatus {
  PENDING
  ASSIGNED
  IN_TRANSIT
  OUT_FOR_DELIVERY
  DELIVERED
  FAILED
  RETURNED
  CANCELLED
}

enum CourierType {
  PATHAO
  REDX
  STEADFAST
  CUSTOM
}

enum RefundStatus {
  REQUESTED
  APPROVED
  REJECTED
  COMPLETED
}

enum BannerType {
  HERO
  BANNER1
  BANNER2
  PROMOTION
  POPUP
}

enum AnalyticsEventName {
  session_start
  page_view
  heartbeat
}

enum ChatStatus {
  OPEN
  IN_PROGRESS
  CLOSED
}

enum ChatPriority {
  LOW
  NORMAL
  HIGH
}

enum AccessScopeType {
  GLOBAL
  WAREHOUSE
}

enum IdentityType {
  NID
  PASSPORT
}

enum DeliveryManStatus {
  PENDING
  ACTIVE
  SUSPENDED
  REJECTED
  RESIGNED
}

enum DeliveryApplicationStatus {
  DRAFT
  SUBMITTED
  UNDER_REVIEW
  APPROVED
  REJECTED
}

enum DeliveryManDocumentType {
  PROFILE_PHOTO

  IDENTITY_FRONT
  IDENTITY_BACK

  FATHER_IDENTITY_FRONT
  FATHER_IDENTITY_BACK

  MOTHER_IDENTITY_FRONT
  MOTHER_IDENTITY_BACK

  REFERENCE_IDENTITY_FRONT
  REFERENCE_IDENTITY_BACK

  BANK_CHEQUE
  BOND
  CONTRACT_PAPER

  OTHER
}

enum DeliveryAssignmentStatus {
  ASSIGNED
  ACCEPTED
  REJECTED
  PICKUP_CONFIRMED
  IN_TRANSIT
  OUT_FOR_DELIVERY
  DELIVERED
  FAILED
  RETURNED
}

enum PickupProofStatus {
  PENDING
  CONFIRMED
}

enum RfqStatus {
  DRAFT
  SUBMITTED
  CLOSED
  AWARDED
  CANCELLED
}

enum RfqInvitationStatus {
  INVITED
  RESUBMISSION_REQUESTED
  RESPONDED
  DECLINED
  AWARDED
}

enum QuotationStatus {
  DRAFT
  SUBMITTED
  WITHDRAWN
}

enum RfqAwardStatus {
  AWARDED
  CONVERTED_TO_PO
  CANCELLED
}

enum ComparativeStatementStatus {
  DRAFT
  SUBMITTED
  MANAGER_APPROVED
  COMMITTEE_APPROVED
  FINAL_APPROVED
  REJECTED
  CANCELLED
}

enum ComparativeStatementApprovalStage {
  DRAFT
  SUBMISSION
  MANAGER_REVIEW
  COMMITTEE_REVIEW
  FINAL_APPROVAL
  REJECTION
  CANCELLATION
}

enum ComparativeStatementApprovalDecision {
  SUBMITTED
  APPROVED
  REJECTED
  CANCELLED
}

enum PurchaseOrderApprovalStage {
  DRAFT
  SUBMISSION
  MANAGER_REVIEW
  COMMITTEE_REVIEW
  FINAL_APPROVAL
  REJECTION
  CANCELLATION
}

enum PurchaseOrderApprovalDecision {
  SUBMITTED
  APPROVED
  REJECTED
  CANCELLED
}

enum PurchaseRequisitionStatus {
  DRAFT
  SUBMITTED
  BUDGET_CLEARED
  ENDORSED
  APPROVED
  REJECTED
  CONVERTED
  CANCELLED
}

enum PurchaseRequisitionApprovalStage {
  PLANNING
  SUBMISSION
  BUDGET_CLEARANCE
  ENDORSEMENT
  FINAL_APPROVAL
  ROUTED_TO_PROCUREMENT
  REJECTION
  CANCELLATION
}

enum PurchaseRequisitionApprovalDecision {
  APPROVED
  REJECTED
}

enum WorkflowNotificationChannel {
  SYSTEM
  EMAIL
}

enum WorkflowNotificationStatus {
  PENDING
  SENT
  FAILED
}

enum PurchaseOrderStatus {
  DRAFT
  SUBMITTED
  MANAGER_APPROVED
  COMMITTEE_APPROVED
  APPROVED
  PARTIALLY_RECEIVED
  RECEIVED
  REJECTED
  CANCELLED
}

enum PurchaseOrderLandedCostComponent {
  FREIGHT
  CUSTOMS
  HANDLING
  INSURANCE
  CLEARING
  OTHER
}

enum GoodsReceiptStatus {
  RECEIVED
  CANCELLED
}

enum GoodsReceiptAttachmentType {
  CHALLAN
  BILL
  OTHER
}

enum GoodsReceiptEvaluatorRole {
  REQUESTER
  PROCUREMENT
  ADMINISTRATION
}

enum SupplierReturnStatus {
  DRAFT
  SUBMITTED
  APPROVED
  PARTIALLY_DISPATCHED
  DISPATCHED
  CLOSED
  CANCELLED
}

enum SupplierSlaEvaluationStatus {
  OK
  WARNING
  BREACH
}

enum SupplierPortalAccessStatus {
  ACTIVE
  SUSPENDED
  REVOKED
}

enum SupplierDocumentVerificationStatus {
  PENDING
  VERIFIED
  REJECTED
  EXPIRED
}

enum SupplierProfileUpdateRequestType {
  PROFILE_UPDATE
  DOCUMENT_UPDATE
  ANNUAL_RENEWAL
}

enum SupplierProfileUpdateRequestStatus {
  PENDING
  APPROVED
  REJECTED
}

enum SupplierFeedbackSourceType {
  INTERNAL
  CLIENT
  VENDOR_SELF
}

enum SupplierPortalNotificationType {
  GENERAL
  DOCUMENT_EXPIRY
  APPROVAL
  RFQ
  WORK_ORDER
  PAYMENT
}

enum SupplierProposalAttachmentType {
  TECHNICAL
  FINANCIAL
  SUPPORTING
}

enum InvestorPortalAccessStatus {
  ACTIVE
  SUSPENDED
  REVOKED
}

enum SupplierSlaSeverity {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum SupplierSlaActionStatus {
  NOT_REQUIRED
  OPEN
  IN_PROGRESS
  RESOLVED
  DISMISSED
}

enum SupplierSlaDisputeStatus {
  NONE
  OPEN
  UNDER_REVIEW
  RESOLVED
  REJECTED
}

enum SupplierSlaTerminationCaseStatus {
  OPEN
  IN_REVIEW
  APPROVED
  REJECTED
  EXECUTED
}

enum SupplierSlaTerminationAction {
  WATCHLIST
  SUSPEND_NEW_PO
  REVIEW_CONTRACT
  TERMINATE_RELATIONSHIP
}

enum SupplierCompanyType {
  PROPRIETOR
  LIMITED_COMPANY
}

enum SupplierDocumentType {
  PROPRIETOR_NID
  TRADE_LICENSE
  PROPRIETOR_DETAILS
  PROPRIETOR_VISITING_CARD
  DECLARATION
  SEAL
  SIGNATURE
  DIRECTORS_NID_DETAILS
  CERTIFICATE_OF_INCORPORATION
  TAX_IDENTIFICATION_NUMBER
  BOARD_RESOLUTION
  VAT_REGISTRATION
  TAX_COMPLIANCE_CERTIFICATE
  BANK_INFORMATION
}

enum ReplenishmentStrategy {
  MIN_MAX
  REORDER_POINT
}

enum SupplierInvoiceStatus {
  POSTED
  PARTIALLY_PAID
  PAID
  CANCELLED
}

enum SupplierInvoicePaymentHoldStatus {
  CLEAR
  HELD
  OVERRIDDEN
}

enum SupplierInvoiceSlaCreditStatus {
  NONE
  RECOMMENDED
  APPLIED
  WAIVED
}

enum SupplierPaymentMethod {
  CASH
  BANK_TRANSFER
  MOBILE_BANKING
  CHEQUE
  ADJUSTMENT
}

enum SupplierLedgerEntryType {
  INVOICE
  PAYMENT
  ADJUSTMENT
}

enum SupplierLedgerDirection {
  DEBIT
  CREDIT
}

enum InvestorStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
}

enum InvestorKycStatus {
  PENDING
  UNDER_REVIEW
  VERIFIED
  REJECTED
}

enum InvestorDocumentType {
  IDENTITY_PROOF
  TAX_IDENTIFICATION
  BANK_PROOF
  ADDRESS_PROOF
  INVESTMENT_AGREEMENT
  SOURCE_OF_FUNDS
}

enum InvestorDocumentVerificationStatus {
  PENDING
  UNDER_REVIEW
  VERIFIED
  REJECTED
  EXPIRED
}

enum InvestorMasterChangeRequestStatus {
  PENDING
  APPROVED
  REJECTED
}

enum InvestorProfileUpdateRequestStatus {
  PENDING
  APPROVED
  REJECTED
}

enum InvestorWithdrawalRequestStatus {
  REQUESTED
  APPROVED
  REJECTED
  SETTLED
  CANCELLED
}

enum InvestorPortalNotificationType {
  PROFILE_UPDATE_REQUEST
  PROFILE_UPDATE_APPROVED
  PROFILE_UPDATE_REJECTED
  WITHDRAWAL_REQUEST
  WITHDRAWAL_STATUS
  DOCUMENT_REVIEW
  PAYOUT_STATUS
  STATEMENT_READY
  SYSTEM
}

enum InvestorPortalNotificationStatus {
  UNREAD
  READ
}

enum InvestorInternalNotificationType {
  PROFILE_REQUEST
  DOCUMENT_REVIEW
  PROFIT_RUN
  PAYOUT
  WITHDRAWAL
  STATEMENT_SCHEDULE
  SYSTEM
}

enum InvestorInternalNotificationStatus {
  UNREAD
  READ
}

enum InvestorStatementScheduleFrequency {
  WEEKLY
  MONTHLY
  QUARTERLY
}

enum InvestorStatementScheduleStatus {
  ACTIVE
  PAUSED
}

enum InvestorStatementDeliveryFormat {
  CSV
  PDF
  BOTH
}

enum InvestorTransactionType {
  CAPITAL_COMMITMENT
  CAPITAL_CONTRIBUTION
  PROFIT_ALLOCATION
  LOSS_ALLOCATION
  DISTRIBUTION
  WITHDRAWAL
  ADJUSTMENT
}

enum InvestorLedgerDirection {
  DEBIT
  CREDIT
}

enum InvestorProfitRunStatus {
  PENDING_APPROVAL
  APPROVED
  REJECTED
  POSTED
}

enum InvestorProfitExpenseAllocationBasis {
  NET_REVENUE
  NET_UNITS
}

enum InvestorProfitPayoutStatus {
  PENDING_APPROVAL
  APPROVED
  REJECTED
  PAID
  VOID
}

enum InvestorPayoutPaymentMethod {
  BANK_TRANSFER
  MOBILE_BANKING
  CHEQUE
  CASH
}

enum ThreeWayMatchStatus {
  PENDING
  MATCHED
  VARIANCE
}

enum WarehouseTransferStatus {
  DRAFT
  SUBMITTED
  APPROVED
  PARTIALLY_DISPATCHED
  DISPATCHED
  PARTIALLY_RECEIVED
  RECEIVED
  CANCELLED
}
