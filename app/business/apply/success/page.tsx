import Link from "next/link";
import { CheckCircle2 } from "lucide-react";
import { Button } from "@/components/ui/button";
export default async function Page({ searchParams }: PageProps<"/business/apply/success">) { const { reference } = await searchParams; return <div className="grid min-h-screen place-items-center bg-slate-50 px-4 dark:bg-slate-950"><div className="max-w-lg rounded-2xl border border-border bg-card p-8 text-center shadow-sm"><CheckCircle2 className="mx-auto size-14 text-emerald-600" /><h1 className="mt-5 text-2xl font-bold">Application received</h1><p className="mt-3 text-sm text-muted-foreground">Your organization is pending verification. Portal access will remain limited until its requested capabilities are approved.</p>{reference && <p className="mt-5 rounded-lg bg-muted px-4 py-3 text-sm">Reference: <span className="font-semibold">{String(reference)}</span></p>}<Button asChild className="mt-6"><Link href="/business">Open business portal</Link></Button></div></div>; }
